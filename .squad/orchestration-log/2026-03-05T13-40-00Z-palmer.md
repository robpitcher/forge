# Orchestration Log — Palmer (DevOps)

**Date:** 2026-03-05T13:40:00Z  
**Agent:** Palmer (DevOps)  
**Mode:** sync  
**Session Result:** COMPLETED

## Work Summary

Reviewed release.yml marketplace publish step added by Copilot. Found and fixed two issues:

1. **Version step ordering:** Version read step was running AFTER package step, preventing `--out` from using the computed version. Moved "Read version" before "Package extension".

2. **Re-build prevention:** Bare `vsce publish` triggers a rebuild from source, creating divergence between GitHub Release artifact and marketplace artifact. Changed to `vsce publish --packagePath forge-$VERSION.vsix` to publish the exact tested .vsix.

## Files Modified

- `.github/workflows/release.yml` — reordered steps, fixed publish command
- `.squad/agents/palmer/history.md` — appended session learnings  
- `.squad/decisions/inbox/palmer-release-marketplace.md` — created decision doc

## Key Decision

**Build once, publish everywhere:** Stable releases must publish the pre-tested GitHub Release .vsix to marketplace, not rebuild from source.

## Status

✅ Complete. All changes committed to copilot/update-release-workflow-for-vscode-marketplace.

