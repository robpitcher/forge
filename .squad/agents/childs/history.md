# Project Context

- **Owner:** Rob Pitcher
- **Project:** Enclave — VS Code extension providing Copilot Chat for air-gapped environments. Uses @github/copilot-sdk in BYOK mode to route inference to private Azure AI Foundry endpoint. No GitHub auth, no internet.
- **Stack:** TypeScript, VS Code Extension API (Chat Participant), @github/copilot-sdk, esbuild, Azure AI Foundry
- **Created:** 2026-02-27

## Key Files

- `src/copilotService.ts` — CopilotClient lifecycle, BYOK session creation, session map
- `src/extension.ts` — Where SDK events (message_delta, session.idle, session.error) are consumed
- `package.json` — @github/copilot-sdk dependency (v0.1.26)
- `specs/PRD-airgapped-copilot-vscode-extension.md` — Product requirements

## Learnings

<!-- Append new learnings below. Each entry is something lasting about the project. -->

📌 Team update (2026-02-27T05:03:36Z): MVP triage complete. Documentation issues #17–19 routed to @copilot for initial draft (docs/setup guide); Childs owns #21, #23–24 (deferred features, air-gap guide, config details). Dependency: #19 must complete before #17 final review. — decided by MacReady

📌 Dependency mapping (2026-02-27): Added `**Dependencies:** #X, #Y` sections to all 22 issues with upstream deps. Format: prepend to body with `---` separator. Used `gh issue edit --body` for each. Issues #2, #25–#29 have no MVP dependencies and were left clean.

📌 Sprint field assignment (2026-02-27): Set Sprint iteration field on all 28 project items via GraphQL `updateProjectV2ItemFieldValue` mutation. Project ID: `PVT_kwHOANBAvc4BQSxx`, Sprint Field ID: `PVTIF_lAHOANBAvc4BQSxxzg-dMUk`. Sprint IDs: Sprint 1 = `09d998ce`, Sprint 2 = `3ac56f5d`, Sprint 3 = `59011026`. GitHub Projects V2 API does not support creating new iterations programmatically — must use the web UI for that. Issues #28/#29 parked in Sprint 3 as fallback until Sprint 4/5 are created manually.

📌 Release branch process (2026-02-27): Branching strategy for releases: create `rel/X.Y.Z` from `dev`, strip `.squad/` via `git rm -r --cached .squad/` + `.gitignore` update, push and open PR to `main`. `.squad/` removal is release-branch-only — dev keeps it tracked. PR #30 opened for v0.2.0 (base: main, head: rel/0.2.0). Rob reviews and squash-merges manually. `.github/agents/` is kept on release branches — needed for GitHub agent recognition. Always `git checkout dev` after pushing release branch.

📌 CopilotClient lifecycle validation (2026-02-27): Issue #4 validation confirms the SDK integration is complete and correct. `getOrCreateClient()` implements singleton pattern for lazy init, respects `cliPath` config option, and handles errors gracefully with `CopilotCliNotFoundError`. The `client.stop()` call in `deactivate()` ensures graceful shutdown. SDK manages stdio transport to CLI internally — no additional stdio wiring needed in our code. PR #34 opened as validation task (no code changes required).

📌 import.meta.resolve CJS bundling fix (2026-02-27): `@github/copilot-sdk` v0.1.26 uses `import.meta.resolve("@github/copilot/sdk")` in `getBundledCliPath()` to locate the CLI binary. esbuild's ESM→CJS conversion replaces `import.meta` with an empty object, breaking this call. Fix: esbuild `define` maps `import.meta.resolve` to a banner-injected polyfill (`__importMetaResolve`) that walks `node_modules` directories to resolve specifiers, bypassing Node.js exports-map restrictions that block `require.resolve()` for ESM-only packages. Both `@github/copilot-sdk` and `@github/copilot` only export via `import` condition — `require.resolve()` cannot resolve them. The polyfill returns `file://` URLs matching native `import.meta.resolve` behavior.

📌 Team update (2026-02-27T22:47Z): Phase 2 issues #25 (Enable CLI Tools) and #26 (Add Workspace Context) rescoped for WebviewView architecture — decided by MacReady. Issue #25 now includes tool confirmation UX requirement (inline approval cards in webview) and auto-approve security setting; routed to Childs (SDK integration) + Blair (webview UX). Issue #26 now specifies custom context capture from VS Code Extension API (no built-in @workspace variable); routed to Blair (Extension API & webview) + Childs (prompt construction). Both significantly expanded with implementation details.

📌 SDK tool API shape (2026-02-27): The SDK uses `onPermissionRequest` callback (not event-based) for tool approval. The handler receives `PermissionRequest { kind, toolCallId?, ... }` and returns `PermissionRequestResult { kind: "approved"|"denied-*" }`. Tool execution lifecycle is observed via session events: `tool.execution_start` (has `toolName`) and `tool.execution_complete` (has `success`, `result`, `error`). Removing `availableTools: []` from `createSession()` enables all default CLI tools. Decision documented in `.squad/decisions/inbox/childs-sdk-tool-events.md`.

📌 Team update (2026-02-28T0000Z): SDK tool approval uses onPermissionRequest callback (sync) + session.on() events. Message protocol defined by Blair. — decided by Childs & Blair

📌 Prompt context format (#26): `_buildPromptWithContext` prepends workspace context as fenced code blocks before the user prompt. Each block has a `--- Context: {path} ({lang}) ---` header followed by a language-tagged fence. Total context capped at 8000 chars; last item truncated with `...[truncated]` note if over budget. User prompt always appended unmodified after a blank line separator. Blair's stub replaced with real implementation; duplicate `ContextItem` interface in types.ts consolidated.

📌 PR #75 review fixes (2026-02-28): Five review items resolved. (1) endLine off-by-one: VS Code selections are end-exclusive — when selection ends at column 0 of next line, we now subtract 1 from endLine. Applied at both `attachSelection` command and `chatFocused` handler. (2) 8000-char budget: separator overhead (`\n\n` between blocks and before prompt) is now counted in `usedChars` so emitted context stays within budget. (3) Context validation: `sendMessage` handler now defensively filters `message.context` items, ensuring each has required string properties before casting to `ContextItem[]`. (4) Stale test comment updated to reflect active `it(...)` tests.

📌 Entra ID auth comments (#27) (2026-02-28): Added `bearerToken?: string` field to `ProviderConfig` in `src/types.ts` with JSDoc explaining the Entra ID / DefaultAzureCredential workaround — SDK only accepts a static string, no refresh callback. Added auth strategy comment block in `src/copilotService.ts` `getOrCreateSession()` explaining API key default, Entra bearer token path, and why session-per-conversation handles token refresh (~1hr lifetime). Both locations include `TODO(#27)` to revisit when SDK adds native Entra support. `tsc --noEmit` passes clean.

📌 Credential provider implementation (#27) (2026-02-28): Created `src/auth/credentialProvider.ts` with `CredentialProvider` interface (`getToken(): Promise<string>`), `EntraIdCredentialProvider` (wraps DefaultAzureCredential with cognitive services scope), `ApiKeyCredentialProvider` (returns static key), and `createCredentialProvider()` factory (dynamic `import("@azure/identity")` for entraId mode). Modified `getOrCreateSession()` signature to accept `authToken: string` — caller resolves token, copilotService just wires it into ProviderConfig as `bearerToken` or `apiKey` based on `config.authMethod`. No errors in own files; test/extension errors expected until Blair's `authMethod` addition to `ExtensionConfig` lands.

📌 Team update (2026-02-28T16:16Z): Design review ceremony for #27 DefaultAzureCredential auth complete. 10 architecture decisions merged into decisions.md. Childs owns credentialProvider.ts (interface, EntraId + ApiKey impls, factory); Blair owns authMethod setting + config wiring; Windows owns test coverage (23 new tests, all pass). Implementation coordinated via contracts and signatures. Ready for user review before merge to dev. — decided by MacReady

📌 Auth status provider (#80) (2026-02-28): Created `src/auth/authStatusProvider.ts` to expose authentication state for UI layer. Exports `AuthState` type (`"authenticated" | "notAuthenticated" | "error"`), `AuthStatus` discriminated union with method/reason/message details, and `checkAuthStatus(config, secrets)` async function. Probes Entra ID via `createCredentialProvider().getToken()` (catches errors, returns state), checks API key existence in SecretStorage, validates endpoint config. Never throws — always returns status object. ~55 lines. Blair owns UI consumption (status icon/menu in chat view).

📌 Team update (2026-02-28T23:39:00Z): Issue #80 — Auth status UX complete. Childs created authStatusProvider.ts with AuthStatus type and checkAuthStatus() function (never throws, probes Entra ID + checks API key, validates endpoint). Blair implemented status bar + webview banner driven by checkAuthStatus(). Windows wrote 15 unit tests. Decision merged to decisions.md. — coordinated by all three

📌 Entra ID error classification fix (2026-02-28): `checkAuthStatus` Entra ID catch block now distinguishes "not logged in" from real errors. Added `isNotLoggedInError()` helper with 8 regex patterns matching common DefaultAzureCredential signals (CredentialUnavailableError, "no credential", "az login", AADSTS codes, etc.). Not-logged-in errors return `{ state: "notAuthenticated", reason: "Sign in with Azure CLI…" }` instead of `{ state: "error" }`. Genuine errors (network, misconfiguration) return a short friendly message instead of raw SDK stack traces. AuthStatus type unchanged — fix is purely classification logic. Tests updated: 4 new test cases for login-signal patterns, 2 existing tests updated to match new behavior. Requested by Rob Pitcher.

---
📌 Team update (2026-03-01T03:14:32Z): Auth UX Fixes completed — Childs' error classification integrated with Blair's sign-in flow. First-run users now see sign-in prompt (not error) when using Entra ID without being logged in. — decided by Childs & Blair

📌 System message customization (#88) (2026-03-01): Added `forge.copilot.systemMessage` setting to `package.json` (multiline string with `editPresentation: multilineText`), `ExtensionConfig` interface, and `getConfiguration()`. In `getOrCreateSession()`, non-empty values are passed to `SessionConfig` as `{ content: value }` which the SDK appends to the default system prompt (append mode). Empty/undefined preserves SDK default behavior — no `systemMessage` key is spread into the config. SDK's `SystemMessageConfig` type requires `{ content: string, mode?: "replace" }` — plain strings are not accepted. PR #95.
📌 Team update (2026-03-01T12:13:49Z): Tool result cards removed from chat UI — redundant noise eliminated. Model's streaming text + tool approval cards sufficient for UX. 122 tests pass, build clean. — decided by Blair

📌 Team update (2026-03-01T12:20:19Z): Context chips in sent messages — UI enhancement renders context attachment history below user messages. Blair's webview changes. No SDK changes needed. — decided by Blair
📌 Tool control settings (#91) (2026-03-01): Added `forge.copilot.availableTools` (whitelist, optional string[]) and `forge.copilot.excludedTools` (blacklist, default: `["url"]`) to `package.json` contributes, `ExtensionConfig` interface, and `getConfiguration()`. In `getOrCreateSession()`, `availableTools` takes precedence when both are set — a `console.warn` fires and only `availableTools` is passed to `SessionConfig`. Empty arrays are passed through (empty `excludedTools` = "exclude nothing"). Validation in `validateConfiguration()` also flags the conflict. Pre-existing test file `src/test/tool-control-settings.test.ts` (19 tests written by Windows) all pass alongside 7 new tests in configuration/copilotService test files. PR #93.

📌 Tool control refactor (#91) (2026-03-01): Simplified tool settings — removed `availableTools` whitelist entirely (from package.json, ExtensionConfig, getConfiguration, validateConfiguration, getOrCreateSession). Now only `excludedTools` exists. Added `enum` items to `excludedTools` in package.json (`["shell","read","write","url","mcp"]` with descriptions) so VS Code renders a dropdown picker instead of free-text. Removed mutual-exclusion validation warning and console.warn. Test count dropped from 19 to 10 in tool-control-settings.test.ts; removed 3 availableTools tests from copilotService.test.ts and 3 mutual-exclusion tests from configuration.test.ts. All 134 tests pass.

📌 Session management wrappers (#87) (2026-03-01): Added SDK session management API wrappers to `src/copilotService.ts` for conversation history persistence. Exported functions: `listConversations()` (wraps `client.listSessions()`, maps `SessionMetadata` → `ConversationMetadata`), `resumeConversation()` (wraps `client.resumeSession()`, applies same provider+tool config as `getOrCreateSession`, stores in sessions map), `getLastConversationId()` (wraps `client.getLastSessionId()`), `deleteConversation()` (wraps `client.deleteSession()`, removes from sessions map). Refactored provider+tool config construction into `buildProviderConfig()` and `buildToolConfig()` helper functions (DRY principle) — shared by both `getOrCreateSession` and `resumeConversation`. Added `ConversationMetadata` interface to `src/types.ts` for UI display. Re-exported `SessionMetadata`, `SessionListFilter`, `ResumeSessionConfig` from SDK. All error paths throw with actionable messages. TypeScript typecheck passes clean.

📌 PR #102 review fixes (#87) (2026-03-01): Two fixes from Copilot PR review. (1) Session ID mismatch: `getOrCreateSession()` now passes `sessionId: conversationId` to `copilotClient.createSession()` so SDK-persisted sessions use the same ID as the extension's conversation tracking — critical for `listSessions`/`resumeSession`/`deleteSession` to find the right session. (2) Unsafe config casts: `listConversations()`, `getLastConversationId()`, and `deleteConversation()` now accept `config: ExtensionConfig` parameter instead of casting `{} as ExtensionConfig`, ensuring `cliPath` and other config is respected. Callers in extension.ts will be updated by Blair.
