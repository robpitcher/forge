# Orchestration: SDK Upgrade @github/copilot-sdk 0.1.26 → 0.1.32

**Agent:** Blair (Extension Dev)  
**Timestamp:** 2026-03-07T16:31:00Z  
**Duration:** ~45 minutes  
**Outcome:** SUCCESS

## What Was Done

### 1. SDK Dependency Bump
- `npm install @github/copilot-sdk@0.1.32`
- Updated `package.json` and `package-lock.json`

### 2. onPermissionRequest Breaking Change Fix
- **File:** `src/copilotService.ts`
  - `buildSessionConfig()`: Now includes `onPermissionRequest` (required field in SDK v0.1.32)
  - `getOrCreateSession()`, `resumeConversation()`: Apply `onPermissionRequest: approveAll` as fallback when not provided
  - Pattern: Functions keep it optional (for backwards-compat with test callers), resolve to `approveAll` internally

### 3. Mock Updates
- **File:** `src/test/__mocks__/copilot-sdk.ts`
  - Added `export const approveAll = vi.fn().mockResolvedValue(...)`
  - Rule applied: Any SDK value imported directly in production must have mock export

### 4. Test Mock Updates (vitest.config.mts)
- `__COPILOT_CLI_VERSION__` reads SDK's @github/copilot dependency at test time
- Updated two tests in `cliInstaller.test.ts`:
  1. "defaults version to SDK's @github/copilot dependency" — mock dep string and expected args updated
  2. "falls back to bundled CLI version" — expected arg updated
- Protocol v3 support confirmed; all SDK event patterns validated

### 5. Test Suite Validation
- `npm run build` — clean esbuild bundle
- `npx tsc --noEmit` — TypeScript strict mode passes
- `npm test` — **315 tests pass** (full suite, no regressions)

## Decisions Merged

1. **SDK Upgrade Pattern** (Blair decision in decisions/inbox/)
   - `onPermissionRequest` default strategy
   - `approveAll` mock export rule
   - `__COPILOT_CLI_VERSION__` maintenance procedure

2. **CLI Status Tooltip** (Blair decision in decisions/inbox/)
   - Status bar tooltip now shows auth + CLI info (Markdown with codicons)
   - No text changes, only tooltip enrichment

## Branch & Commit

- **Branch:** `squad/sdk-upgrade-0.1.32`
- **Base:** `dev`
- **Status:** Ready for PR

## Context

- **Prior issue:** CLI v1.0.2 runs protocol v3; SDK v0.1.26 expects v2 → protocol mismatch crash on startup
- **Solution:** SDK v0.1.32 adds protocol v3 support
- **Integration:** No other breaking changes affect Forge's BYOK provider config, event handling, or lifecycle

## Risk Assessment

**LOW** — All core patterns (BYOK config, named events, client lifecycle, CLI binary resolution) remain unchanged. Breaking change (onPermissionRequest required) is localized and handled with approveAll fallback.

## Blockers

None. All tests passing, branch ready for merge.
