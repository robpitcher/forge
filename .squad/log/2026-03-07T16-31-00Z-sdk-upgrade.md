# Session Log: SDK Upgrade 0.1.26 → 0.1.32

**Agent:** Blair  
**Date:** 2026-03-07T16:31:00Z

## Summary

Upgraded `@github/copilot-sdk` from 0.1.26 to 0.1.32 to resolve protocol v3 mismatch with CLI 1.0.2.

## What Happened

1. **npm install** @github/copilot-sdk@0.1.32
2. **Fixed onPermissionRequest** — now required in SessionConfig; applied `approveAll` fallback in `getOrCreateSession()` and `resumeConversation()`
3. **Mock updates** — added `approveAll` export to test mocks
4. **vitest.config.mts** — verified `__COPILOT_CLI_VERSION__` reads SDK's @github/copilot dep; updated two tests in cliInstaller.test.ts
5. **Tests:** 315 tests pass, build clean

## Outcome

✅ SDK upgraded, all tests passing, branch `squad/sdk-upgrade-0.1.32` ready for PR to `dev`.

## Decisions

- SDK Upgrade Pattern (onPermissionRequest strategy, approveAll mock)
- CLI Status Tooltip (markdown status bar with codicons)
