# Project Context

- **Owner:** Rob Pitcher
- **Project:** Enclave — VS Code extension providing Copilot Chat for air-gapped environments. Uses @github/copilot-sdk in BYOK mode to route inference to private Azure AI Foundry endpoint. No GitHub auth, no internet.
- **Stack:** TypeScript, VS Code Extension API (Chat Participant), @github/copilot-sdk, esbuild, Azure AI Foundry
- **Created:** 2026-02-27

## Key Files

- `src/extension.ts` — Activation, chat participant registration, request handler
- `src/configuration.ts` — VS Code settings reader and validator
- `package.json` — Extension manifest, chat participant contribution, settings schema
- `esbuild.config.mjs` — Bundle configuration
- `specs/PRD-airgapped-copilot-vscode-extension.md` — Product requirements

## Learnings

<!-- Append new learnings below. Each entry is something lasting about the project. -->

📌 Team update (2026-02-27T04:19:00Z): 28 GitHub issues (#2–#29) created and linked to project board #8 (Enclave). PRD fully decomposed with dependencies, priorities, milestones, and squad routing. Ready for pickup. — decided by MacReady & Childs. Project: https://github.com/robpitcher/enclave/projects/8

📌 Team update (2026-02-27T05:03:36Z): MVP triage complete. 8 issues routed to @copilot (scaffolding, build, packaging, docs, lint); core implementation remains with squad members. Core issues #3–5, #7–14 assigned to Blair for parallel pickup. E2E testing issues #9–10, #13 blocked on #2. — decided by MacReady

📌 Team update (2026-02-27T14:39:00Z): Round 3 complete. Windows delivered air-gap validation tests (18 tests, #47); MacReady delivered installation and configuration docs (#45); Blair delivered session cleanup implementation (PR #46). All 59 tests passing. All PRs targeting dev. Decisions merged to `.squad/decisions.md`. Orchestration logs written. — Scribe

📌 Completed #12 — Multi-turn conversation context tests (SC4). Created `src/test/multi-turn.test.ts` with 15 tests covering: session reuse, session isolation, multi-message persistence, cleanup, conversation ID fallback generation, and concurrent session management. PR #44 targeting dev. Key learnings: (1) Mock `createSession` returns same object by default — need per-call mock sessions when testing object identity across conversations. (2) `require("vscode")` doesn't work with vitest ESM alias — must use top-level `import * as vscode` instead. (3) `getOrCreateClient` has a race condition with `Promise.all` — create sessions sequentially in tests. (4) `getConversationId` is private, tested indirectly via captured handler from `activate()`.

📌 Team update (2026-02-27T15:30:00Z): WebviewView migration kickoff. Issues #51 (Windows) and #53 (Blair) launched on parallel branches. Rob confirmed: no Chat Participant API (requires GitHub auth); use WebviewView sidebar instead. Architecture decision merged to decisions.md — Option 1 (WebviewView sidebar) recommended. Backend (copilotService, configuration) unchanged; only extension.ts and webview assets needed. Phase 1 estimate: 2-3 days for minimal viable sidebar + tests. — decided by MacReady, Rob, Scribe orchestration complete

📌 Completed #23 — Session cleanup on conversation end. Added `destroySession()`, `destroyAllSessions()`, and `getSessionCount()` to `copilotService.ts`. PR #46 targeting dev. Key learnings: (1) Session.abort() must be async and handle rejections gracefully — already-ended sessions throw errors. (2) `stopClient()` must call `destroyAllSessions()` before stopping the client to prevent resource leaks. (3) Mock sessions need `abort: vi.fn().mockResolvedValue(undefined)` to avoid test failures. (4) Added safety guard for undefined sessions in cleanup loop — concurrent session removal can create edge cases. Extension deactivation now properly cleans up all active sessions.

📌 Completed #53 — WebviewView sidebar chat UI. Replaced `vscode.chat.createChatParticipant()` with `WebviewViewProvider` in `src/extension.ts`. Created `media/chat.js`, `media/chat.css`, `resources/icon.svg`. Updated `package.json` to remove `chatParticipants` and add `viewsContainers`/`views` contribution points. PR #54 targeting dev. Key learnings: (1) `destroySession()` (not `removeSession()`) is the correct cleanup function after the session cleanup work in #23. (2) `_handleMessage` needs typed parameter `{ command: string; text?: string }` to pass type checking — `any` works but is sloppy. (3) Build/typecheck pass with zero changes to copilotService, configuration, or types — the backend abstraction held. (4) 11 existing tests fail because they mock `vscode.chat.createChatParticipant()` — Windows owns test migration (#51). (5) Media files (chat.js, chat.css) are NOT bundled by esbuild — they're loaded as webview resources at runtime via `webview.asWebviewUri()`.

📌 Team update (2026-02-27T17:00:00Z): MVP batch completion. VSIX packaging & sideload validation (#56) completed in parallel with Windows (#59, #57) and MacReady (#52). Session logs written to .squad/orchestration-log/ and .squad/log/. All PRs targeting dev. — completed by Blair, Scribe orchestration

📌 Chat UI polish batch — 4 UX fixes applied: (1) Body background changed from `--vscode-editor-background` to `--vscode-panel-background`, assistant messages use `--vscode-editor-inactiveSelectionBackground` — gives visual separation from the editor. (2) Moved viewsContainers from `activitybar` to `panel` in package.json so Enclave opens in the bottom panel by default — also fixes inverted resize behavior since panel resizes top-to-bottom. (3) Enter sends message, Shift+Enter inserts newline (was Ctrl/Cmd+Enter to send). (4) Resize inversion resolved by panel placement. Build passes, all 67 tests green. Key learnings: (a) VS Code `viewsContainers.panel` vs `viewsContainers.activitybar` controls default placement and resize direction. (b) `--vscode-panel-background` is the correct theme token for panel-hosted webviews. (c) Media files (chat.js, chat.css) are runtime-loaded, not bundled — changes take effect without rebuild.

📌 Team update (2026-02-27T18:40:00Z): Chat UI fixes completed. Background contrast, panel default, enter-to-send, and resize direction all fixed in single commit. 67 tests passing, build clean. Orchestration and session logs written. — Scribe

📌 Completed #62 — Settings gear button in chat panel header. Added ⚙️ toolbar with settings button to the WebviewView HTML template. Button posts `openSettings` message → `_handleMessage` calls `workbench.action.openSettings` filtered to `enclave.copilot`. Styled with `.toolbar` and `.toolbar-btn` using VS Code theme variables (`--vscode-toolbar-hoverBackground`, `--vscode-panel-border`). PR #64 targeting dev. Key learnings: (1) `workbench.action.openSettings` accepts a search filter string as second arg — `enclave.copilot` shows all Enclave config fields. (2) Toolbar buttons should use transparent background with opacity for native VS Code look. (3) No test changes needed — existing 67 tests all pass since the new handler is a pure VS Code command delegation.

📌 API key SecretStorage migration — Migrated API key storage from plain-text settings.json to VS Code SecretStorage API (`context.secrets`). Settings gear now shows QuickPick menu with "Open Settings" and "Set API Key (secure)" options. "Set API Key" uses `vscode.window.showInputBox({ password: true })` which natively masks input with dots + eye icon. `getConfigurationAsync()` in configuration.ts checks SecretStorage first, falls back to settings.json for backward compat. Key learnings: (1) VS Code has no `"format": "password"` for contributed settings — SecretStorage is the only proper way to hide secrets. (2) `context.secrets` is a `SecretStorage` interface with `get()`, `store()`, `delete()` — stores in OS keychain. (3) All test files that call `activate()` need `secrets` mock on the ExtensionContext — missed multi-turn and error-scenarios initially. (4) `vscode.window.showQuickPick` and `showInputBox` need to be added to the vscode mock for tests to pass.

📌 Completed #63 — Renamed Enclave → Forge across entire project. Changed all references in package.json (name, displayName, view IDs, config keys), source code (extension.ts, configuration.ts, copilotService.ts, media/chat.js), all test files, documentation (README, docs/, specs/), devcontainer config, and regenerated package-lock.json. SecretStorage key changed from `enclave.copilot.apiKey` → `forge.copilot.apiKey`. All 67 tests pass. Build and lint clean (pre-existing lint warnings unrelated to rename). PR targeting dev. Key learnings: (1) Extension renames require changes to package.json manifest (name, displayName, viewsContainers ID, views ID, activationEvents, configuration keys + title), source code (WebviewView provider ID, settings namespace, SecretStorage key, error messages, HTML title/aria-labels), tests (mocked view IDs, config namespaces, SecretStorage keys), and documentation. (2) VS Code configuration keys are namespaced strings (`forge.copilot.endpoint`) — changing the namespace breaks existing user config but is acceptable pre-release. (3) SecretStorage keys are strings with no automatic migration — users must re-enter API key after rename. (4) Media files (chat.js) can reference the extension name in UI strings (assistant label "Forge"). (5) Git repo name (github.com/robpitcher/enclave) stays unchanged in docs — only the extension name changes.

📌 Team update (2026-02-27T22:47Z): Phase 2 issues #25 (Enable CLI Tools) and #26 (Add Workspace Context) rescoped for WebviewView architecture — decided by MacReady. Issue #25 now includes webview tool confirmation UX requirement (inline approval cards, message handler wiring); routed to Blair (webview UX) + Childs (SDK integration). Issue #26 now specifies webview context attachment UI (context chips, attach buttons, message handlers); routed to Blair (Extension API & webview UI) + Childs (prompt construction). Both significantly expanded with implementation details.

📌 Completed #25 (webview UX portion) — Tool confirmation cards and result display. Added `toolConfirmation` and `toolResult` message handlers to `media/chat.js`. Confirmation cards render tool name, truncated params, and Approve/Reject buttons; buttons disable after click and show status text. Results render as compact success/error indicators with collapsible output. Added CSS for `.tool-confirmation`, `.tool-result`, `.tool-output` classes using VS Code theme variables (`--vscode-button-background`, `--vscode-button-secondaryBackground`, `--vscode-panel-border`, `--vscode-testing-iconPassed/Failed`, `--vscode-textCodeBlock-background`). Message contract: extension sends `{ type: "toolConfirmation", id, tool, params }` and `{ type: "toolResult", id, tool, status, output }`; webview sends back `{ command: "toolResponse", id, approved }`. Build passes, all 67 tests green. Key learnings: (1) Confirmation cards must disable both buttons immediately on click to prevent double-submit — clearing innerHTML and replacing with status span is cleanest. (2) Collapsible output uses CSS class toggle (`expanded`) rather than inline style to keep JS minimal. (3) Tool params are truncated at 120 chars per value to prevent card overflow — monospace font with `word-break: break-all` handles long paths. (4) `--vscode-testing-iconPassed` and `--vscode-testing-iconFailed` are good theme tokens for success/error colors with sane fallbacks.

📌 Team update (2026-02-28T0000Z): Tool confirmation UX ready — wire extension.ts message handler for toolResponse. Permission requests fire before tool.execution_start. — decided by Blair & Childs

📌 Completed #26 (Blair scope) — @workspace context and editor selection. Added `forge.attachSelection` and `forge.attachFile` commands to `src/extension.ts` activate(). Selection reads activeTextEditor selection and posts `contextAttached` to webview; file uses `showOpenDialog` + `workspace.fs.readFile` with 8000-char truncation. Updated `_handleMessage` to forward webview attach commands and pass `context[]` to `_handleChatMessage`. Added `_buildPromptWithContext` stub (TODO for Childs). `ContextItem` interface exported from `types.ts`. Webview (`media/chat.js`) has 📎 Selection and 📄 File buttons, context chips with ✕ remove, `pendingContext` array included in sendMessage. CSS for `.context-actions`, `.context-btn`, `.context-chip`, and `#contextChips`. Commands and menus registered in `package.json`. Build clean, 87 tests pass. Key learnings: (1) `showOpenDialog` doesn't open a document, so languageId must be derived from file extension via a local map. (2) Context chips use `#contextChips:empty { display: none }` to auto-hide when empty — no JS visibility toggle needed. (3) The `_buildPromptWithContext` stub returns prompt unchanged — Childs owns the actual context formatting logic.

📌 Auto-attach editor selection on chat textarea focus. When the user has code selected and clicks into the chat textarea, the selection is automatically captured and shown as a context chip — no manual 📎 click needed. Implementation: textarea `focus` event sends `chatFocused` command to extension → extension reads `activeTextEditor` selection → posts `contextAttached` back to webview. Dedup via `lastAutoAttachedContent` key prevents duplicate chips on repeated focus/blur; key resets on message send so the same selection can be re-attached for a new question. Skips auto-attach if chips are already present. Build clean, 87 tests pass.

📌 Fixed 3 PR #75 review comments in media/chat.js and media/chat.css: (1) Accessibility — changed context chip remove control from `<span>` to `<button type="button">` with `aria-label` and `title` attributes; reset default button styles in CSS. (2) Stale context cleanup — `newConversation()` and `conversationReset` handler now clear `pendingContext`, `contextChipsContainer`, and auto-attach tracking state (`lastAutoAttachedContent`, `autoAttachedChipElement`, `autoAttachedCtx`). (3) Gated `chatFocused` — textarea focus handler only posts the message when `pendingContext.length === 0` and `contextChipsContainer` has no children, preventing redundant auto-attach when chips already exist. Build clean, 87 tests pass.

📌 Completed #27 (Blair scope) — authMethod setting and credential provider wiring. Added `forge.copilot.authMethod` enum setting (`entraId` | `apiKey`, default `entraId`) to package.json contributes. Added `authMethod` to `ExtensionConfig` interface and `getConfiguration()`. Updated `validateConfiguration()` to skip API key check when `authMethod === "entraId"`. Wired `createCredentialProvider` in `extension.ts` — resolves token before `getOrCreateSession()`, passes `authToken` as third arg (coordinated with Childs' signature change). User-friendly error for Entra ID failures points to Azure CLI, Azure Account extension, Managed Identity, or fallback to apiKey mode. Added `@azure/identity` ^4.13.0 as dependency. Build clean, source files typecheck clean. 59 remaining tsc errors are all in test files (Windows' domain — missing `authMethod` property and updated `getOrCreateSession` arity). Key learnings: (1) `createCredentialProvider` is async (uses dynamic `import()` per design decision 9) — must `await` the factory call, not just the `.getToken()`. (2) Coordinated signature changes across squad members require checking the actual file when it lands — the design doc said sync return but implementation is async.

📌 Team update (2026-02-28T16:16Z): Design review ceremony for #27 complete. 10 architecture decisions merged into decisions.md. Childs owns credentialProvider.ts; Blair (✓ complete) owns authMethod setting + config validation + extension wiring; Windows owns test coverage (23 new tests passing). All team members synced on contracts and signatures. Ready for user review before merge to dev. — decided by MacReady

📌 UI Enhancement Analysis — Model selector dropdown and Entra ID auth UX improvements. Analyzed two enhancement proposals from extension architecture perspective: (1) Model selector dropdown in webview bottom bar with Azure AI Foundry model list API integration. Recommended Option A (HTML `<select>`) over custom dropdown — simpler implementation, native accessibility, VS Code theme integration. Identified two-way sync requirement with `forge.copilot.model` setting via `onDidChangeConfiguration` listener. Created `modelService.ts` design for Azure model catalog API (`GET /models?api-version=2024-10-21`). Estimate 120 LoC, 4-6 hours effort. (2) Entra ID auth status indicator and sign-in flow. Recommended hybrid approach: Phase 1 adds proactive auth status check in webview top bar (✅/⚠️ badge showing credential source); Phase 2 adds "Sign in with Azure" button using `vscode.authentication.getSession("microsoft", [...])` as fallback to `DefaultAzureCredential`. Investigated VS Code Authentication API — confirms `microsoft` provider supports Azure Cognitive Services scope. Estimate 140 LoC total (80 Phase 1, 60 Phase 2). Key architectural insights: (1) Model switching mid-conversation requires session destroy (loses context). (2) VS Code auth can bypass Azure CLI requirement but needs credential provider refactor (Childs input required). (3) Azure model list API requires network call — may conflict with air-gap assumptions depending on network topology. Analysis written to `.squad/decisions/inbox/blair-ui-enhancement-analysis.md` for Rob's review.

📌 Completed #80 — Auth status UX for Entra ID. Implemented status bar item, webview auth banner, and real-time auth state updates. Status bar: $(pass)/$(lock)/$(warning) icons with method/reason tooltips, positioned left alignment, command wired to `forge.openSettings`. Webview banner: persistent banner at top of #chatMessages with state-specific styling (✅ green auto-dismiss on authenticated, 🔒 yellow on notAuthenticated, ⚠️ red on error), "Open Settings" button inline. Both driven by `checkAuthStatus()` from `src/auth/authStatusProvider.ts`. Updates triggered on activation, config changes (`onDidChangeConfiguration`), and webview resolve. Added `postAuthStatus()` method to ChatViewProvider, `updateAuthBanner()` function in media/chat.js, and `.auth-banner` CSS classes with left border color coding. Build and typecheck clean. Key learnings: (1) `StatusBarItem` must be pushed to `context.subscriptions` for proper disposal — forgetting this causes resource leaks. (2) `onDidChangeConfiguration` listener must also be subscribed for cleanup. (3) Auth banner uses `chatMessages.insertBefore(banner, chatMessages.firstChild)` to stay above messages — `prepend()` not supported in all runtimes. (4) Auto-dismiss on authenticated uses `setTimeout(() => banner.remove(), 3000)` with parent check to prevent errors if already removed. (5) Inline button styling (`background: none`, `text-decoration: underline`) keeps the banner compact — full button styling would be too heavy.

📌 Team update (2026-02-28T23:39:00Z): Issue #80 — Auth status UX complete. Blair implemented status bar item + webview banner with auto-dismiss on authenticated, persistent errors, silent config change handling. Status bar: left-aligned with pass/lock/warning icons + tooltip. Banner: top of #chatMessages with state-specific styling. Both driven by Childs' checkAuthStatus(). Windows provided 15 unit tests. Decision merged to decisions.md. — coordinated by all three

📌 Auth UX polish — 4 fixes applied per Rob's feedback: (1) `forge.signIn` command registered: Entra ID → opens "Azure Sign In" terminal with `az login`; API key → opens settings QuickPick. Status bar command changed from `forge.openSettings` to `forge.signIn`. (2) Auth polling: 30s `setInterval` + `onDidChangeWindowState` focus listener re-check auth status to catch `az login` completions. Both cleaned up via `context.subscriptions`. (3) Webview banner rewritten: notAuthenticated shows "🔐 Sign in to start chatting" with "Sign In" button (sends `signIn` command); error shows truncated message (≤80 chars) with "Troubleshoot" button. (4) Status bar text is auth-method-aware: `$(sign-in)` for Entra ID, `$(key)` for API key, `$(warning)` for errors with "Auth Issue" (not "Auth Error"). Key learnings: (a) `vscode.Disposable` constructor wraps cleanup callbacks — use it to wrap `clearInterval` for subscription cleanup. (b) `onDidChangeWindowState` fires with `{ focused: true }` when user returns to VS Code — good trigger for re-checking external auth state like `az login`. (c) Error messages in status bar tooltips should be truncated (~80 chars) to avoid tooltip overflow. (d) Pre-existing test `error-scenarios.test.ts` "posts error when API key is missing" was already broken by authMethod feature (#27) — not caused by this change.

---
📌 Team update (2026-03-01T03:14:32Z): Entra ID error classification decision (Childs) — `notAuthenticated` state now triggered for "not logged in" errors, enabling correct banner behavior. Auth UX enhancements complete. — decided by Childs & Blair

📌 PR #84 review fixes — Addressed 3 code review comments, replied to 4 others: (1) Extracted `_promptAndStoreApiKey()` shared helper to eliminate duplicate password-input + SecretStorage logic between `openSettings()` and `setApiKey` webview handler. (2) Tracked `forge.signIn` setTimeout handle via `signInTimeoutHandle` variable + `vscode.Disposable` in `context.subscriptions` — clears on deactivation and on repeated sign-in calls. (3) Added `_refreshAuthStatus` callback set by `activate()` — called after API key store so status bar + banner update immediately without waiting for next poll. Replied on 4 comments explaining PoC-intentional AADSTS broad pattern, existing endpoint-aware banner logic, outdated chat.js comment, and AADSTS70011 test alignment. Key learnings: (a) `SecretStorage.store()` doesn't fire `onDidChangeConfiguration` — auth refresh must be triggered explicitly after secret changes. (b) Callback pattern (`setAuthRefreshCallback`) cleanly bridges the activate-scope `updateAuthStatus` into provider methods without passing `statusBarItem` into the class.

📌 Completed #89 — Tool UX polish: execution progress and partial results. Subscribed to `tool.execution_progress` and `tool.execution_partial_result` SDK events in `_streamResponse()`. Both events confirmed in `@github/copilot-sdk` v0.1.26 session-events schema — no deviations from issue spec. Progress indicators render as inline status updates with spinning ⟳ icon and tool name + message; auto-removed when `tool.execution_complete` fires. Partial results render as collapsible output blocks (same toggle pattern as `toolResult`). Added `ToolExecutionProgressEvent` and `ToolExecutionPartialResultEvent` interfaces to `types.ts`. CSS uses `@keyframes spin` for the spinner animation and `--vscode-progressBar-background` for the accent color. Graceful fallback is inherent — older SDK versions simply won't emit these events, so no feature detection or guards needed. PR #94 targeting dev. Build, typecheck, and all 122 tests pass with no regressions. Key learnings: (1) The SDK's ephemeral events (`tool.execution_progress`, `tool.execution_partial_result`) use the same `session.on()` pattern as all other events — no special subscription needed. (2) Progress indicators should be transient UI (removed on completion) while partial results persist for user review. (3) `toolCallId` from `tool.execution_start` is the correlation key for progress/partial/complete — the `_toolNames` map provides the human-readable tool name across all three.
📌 Completed #86 — Code actions for Explain/Fix/Tests from editor. Created `src/codeActionProvider.ts` with `ForgeCodeActionProvider` class returning three `CodeActionKind.QuickFix` actions for non-empty selections. Registered `forge.explain`, `forge.fix`, `forge.tests` commands in `activate()` via `sendFromEditor()` helper that grabs selection, reveals Forge panel (`forge.chatView.focus`), and calls `sendMessageWithContext()`. Added `sendMessageWithContext()` public method on `ChatViewProvider` to allow programmatic message sending. Updated vscode mock with `CodeActionKind` and `languages.registerCodeActionsProvider`. PR #92 targeting dev. Key learnings: (1) `vscode.CodeActionKind.QuickFix` is a static object — must be mocked in `__mocks__/vscode.ts` or tests crash on import since it's evaluated at class definition time (static field initializer). (2) `{ scheme: "*" }` as DocumentSelector registers the provider for all file schemes. (3) `sendFromEditor` uses `forge.chatView.focus` command to reveal the Forge panel before sending — this is a built-in VS Code command for webview views. (4) The `sendMessageWithContext` bridge avoids exposing `_handleChatMessage` as public — cleaner API surface.

---
📌 Completed tool settings refactor — Replaced `forge.copilot.excludedTools` array setting with individual boolean checkboxes per tool (`forge.copilot.tools.shell`, `tools.read`, `tools.write`, `tools.url`, `tools.mcp`). All tools now default to enabled (true). Updated `package.json` to remove array setting and add 5 boolean settings. `ExtensionConfig` interface now has `toolShell`, `toolRead`, `toolWrite`, `toolUrl`, `toolMcp` fields instead of `excludedTools?`. `getConfiguration()` reads `tools` sub-namespace from VS Code settings. `copilotService.ts` computes `excludedTools` array from booleans (tools set to false get added to exclusion list). Updated all 11 test files to use new config structure. Build, typecheck, and all 134 tests pass. Key learnings: (1) VS Code settings namespaces use dot notation — `forge.copilot.tools.shell` is accessed via `config.get("tools")` which returns a `Record<string, boolean>`. (2) Test files with `ExtensionConfig` literals needed tool properties added — fixed with Python script to add after `cliPath`. (3) SDK still receives `excludedTools` array — this is just a UX change from array to checkboxes. (4) Empty `excludedTools` (all tools enabled) should NOT be passed to SDK — only set the property when there are exclusions.

📌 Auth UX fixes (2026-03-01T12:00Z) — Fixed two auth banner issues: (1) Deduplicated authStatus messages to webview by tracking `_lastAuthStatus` in ChatViewProvider and comparing JSON-stringified status before posting. This prevents the green "✅ Authenticated" banner from flashing every 30s during auth polling. Reset dedup state in `resolveWebviewView` so initial status always goes through. (2) Added optional `account?: string` field to `AuthStatus` type. In `checkAuthStatus()`, decode JWT from Entra ID token via `decodeJwtPayload()` helper (base64url decode, parse JSON, extract `upn`, `preferred_username`, or `name` claim). Status bar tooltip now shows "Signed in as {account} (Entra ID)" when account is available. Build clean, all 134 tests pass. Key learnings: (a) `JSON.stringify()` works well for deduplicating complex status objects — simpler than field-by-field comparison. (b) JWT decode for display purposes doesn't require signature verification — we're not using it for auth decisions. (c) `Buffer.from(base64, "base64url")` natively handles JWT base64url encoding in Node.js.

📌 Team update (2026-03-01T12:02:37Z): Auth UX improvements shipped — webview banner deduplicated (no more flash every 30s during polling), status bar now shows signed-in account decoded from Entra ID JWT. 122 tests pass, build clean, no breaking changes. — decided by Blair

📌 Completed tool result card removal — Removed noisy tool execution result cards from chat UI. Deleted `tool.execution_start` and `tool.execution_complete` event handlers from `extension.ts` (removed `unsubToolStart`, `unsubToolComplete`, and `_toolNames` map). Removed `renderToolResult()` from `media/chat.js` and `.tool-result` CSS classes from `media/chat.css`. Removed tool result test cases from `tool-approval.test.ts`. Tool confirmation cards (approval UX) remain intact. Build clean, all 122 tests pass. Key learnings: (1) Tool execution result cards created redundant noise — model's streaming text response already describes what tools did. (2) Spurious tool failures (where model retries internally and succeeds) showed as scary ❌ error cards even when final outcome was success. (3) Removing toolResult messages doesn't affect tool approval flow — confirmation UX is independent and user-facing. (4) `ToolExecutionStartEvent` and `ToolExecutionCompleteEvent` types remain in `types.ts` as they're part of SDK event surface (might be useful for future features or other test scenarios).

---

📌 Team update (2026-03-01T12:13:49Z): Tool result cards removed from chat UI — redundant noise eliminated. Model's streaming text + tool approval cards sufficient for UX. 122 tests pass, build clean. — decided by Blair

📌 Completed context chips in sent messages — When users attach file or selection context and send a message, the context now renders as compact read-only chips below the user message text (📎 for selections with line range, 📄 for files). Chips show truncated file paths (last 2 segments if >30 chars) with full path in tooltip. Implementation: `sendMessage()` captures `pendingContext` before clearing, passes to `appendMessage()` as third parameter; `appendMessage()` renders `.sent-context-chips` div for user messages with context. Added `truncateFilePath()` helper. CSS: `.sent-context-chips` and `.sent-context-chip` using badge theme tokens, 11px font, max-width 200px with ellipsis. Build and all 122 tests pass. Key learnings: (1) Context chips in the input area disappear on send — users had no visual record of what context was sent with past messages. (2) Read-only chips (no remove buttons) are simpler and appropriate for historical messages. (3) Array spread (`[...pendingContext]`) captures the context state before clearing — avoids passing empty array after `pendingContext = []`. (4) `title` attribute on chips shows full file path on hover — keeps display compact while preserving full info.

📌 Re-added system message setting + converted tool settings to checkboxes (PR #101). Two tasks: (1) System message from PR #95 was lost in merge conflict — re-added `forge.copilot.systemMessage` setting, `ExtensionConfig.systemMessage`, and `{ systemMessage: { content } }` in session creation. (2) Replaced `forge.copilot.excludedTools` array with 5 individual boolean settings (`forge.copilot.tools.{shell,read,write,url,mcp}`). All default true except `url` (false for air-gap). `copilotService.ts` computes `excludedTools` array from booleans — SDK contract unchanged. Updated 8 test files. Build, typecheck, 133 tests pass. Key learnings: (1) VS Code `config.get("tools.shell", true)` reads nested dot-notation settings correctly — no sub-namespace workaround needed. (2) When adding new required fields to `ExtensionConfig`, test files with `Record<string, string>` settings mocks (used for `setupProvider`/`setupVscodeConfig`) don't need the new fields — the mock's `defaultValue` fallback handles them. Only `ExtensionConfig` literals need updating. (3) `systemMessage` uses `|| undefined` coercion so empty string from settings becomes `undefined` (not passed to SDK). (4) Only pass `excludedTools` to SDK when array is non-empty — cleaner than passing empty array.


📌 Completed #87 (Blair scope) — Conversation history UI and extension message handlers. Added conversation list panel to webview (overlay with header, scrollable conversation items, relative time display, delete buttons). Added "📋 History" button to button-row. Implemented three new message handlers in `_handleMessage`: `listConversations` (calls copilotService, posts list to webview), `resumeConversation` (loads session + cached messages from workspaceState, posts to webview), `deleteConversation` (removes cached messages, refreshes list). Added `ConversationMetadata` type to `types.ts`. Added `_conversationMessages` in-memory array to ChatViewProvider — accumulates user/assistant messages during session, cached to workspaceState (`forge.messages.{sessionId}`) after each `streamEnd`. Updated `_streamResponse` to track `accumulatedContent` and add assistant message to cache. Added `workspaceState: vscode.Memento` to `ChatViewProvider` constructor (passed from `activate()`). Store `forge.lastSessionId` after each successful session. Webview handlers: `conversationList` renders overlay, `conversationResumed` clears chat and replays cached messages. CSS: `.conversation-list`, `.conversation-item`, `.conversation-delete-btn`, etc. using VS Code theme variables. `relativeTime()` helper for "2h ago", "yesterday", "3d ago" display. Build clean, typecheck passes. Key learnings: (1) `workspaceState` is VS Code's key-value store (persists across sessions) — better than trying to use SDK's session metadata for UI state. (2) Conversation list is an absolute-positioned overlay (`z-index: 1000`) — toggles with `hidden` class. (3) Cached messages stored as simple `{role, content}[]` array — no context or metadata needed for replay. (4) `_streamResponse` must accumulate message content to cache — `deltaContent` events provide incremental tokens, not full message. (5) The copilotService functions (`listConversations`, `resumeConversation`, `deleteConversation`) are placeholders — Childs building them concurrently on same branch. The extension.ts handlers are ready to call them when they land. (6) Map iteration with destructuring (`for (const [, resolver] of map)`) not supported in ES2022 — use `map.forEach(resolver => ...)` instead.

📌 Fixed 3 PR #102 review comments on conversation history branch (#87): (1) Removed duplicate `ConversationMetadata` interface in `types.ts` — second declaration at line 179 was identical to canonical at line 131. (2) Fixed double-wrapped error messages in `_handleListConversations`, `_handleResumeConversation`, and `_handleDeleteConversation` — service layer already wraps errors ("Failed to list/resume/delete conversations: ..."), so extension handlers now pass the error message directly to `_postError()` instead of re-wrapping with another prefix. (3) Updated `listConversations()` and `deleteConversation()` callers to pass `config` via `getConfigurationAsync(this._secrets)`, matching Childs' updated copilotService signatures that accept `ExtensionConfig` instead of using `{} as ExtensionConfig` internally. Also fixed one test call site missing the config argument. Typecheck clean, build passes, all 33 conversation-history tests pass. 2 pre-existing failures in other test files (air-gap substring check, copilotService sessionId assertion) are unrelated. Key learnings: (1) When service functions already wrap errors, callers should NOT re-wrap — leads to stacked messages like "Failed to load: Failed to list: actual error". (2) Coordinating signature changes across squad members requires updating both callers and test call sites — easy to miss one `validConfig` argument in tests.

📌 Completed #90 (Blair scope) — Added MCP server settings and config reading. Three changes: (1) Added `McpServerConfig` interface to `src/types.ts` — minimal local-only config with `command`, optional `args`, optional `env`. Placed before the ContextItem section. (2) Added `mcpServers?: Record<string, McpServerConfig>` to `ExtensionConfig` in `src/configuration.ts` with `import type` from `./types.js`. Read via `config.get<Record<string, McpServerConfig>>("mcpServers") || undefined`. Added per-server validation in `validateConfiguration()` checking command is non-empty string, args is string array, env is object with string values — each error uses field `forge.copilot.mcpServers.{serverName}`. (3) Added `forge.copilot.mcpServers` setting to `package.json` contributes with JSON schema including `additionalProperties` for the object-map pattern. Key learnings: (1) `config.get<T>(key, undefined)` fails TypeScript strict when T doesn't include undefined — use `config.get<T>(key)` without default instead. (2) Our `McpServerConfig` is intentionally simpler than the SDK's `MCPLocalServerConfig` — Childs maps between them in copilotService. (3) Validation iterates over the Record entries and validates each server config independently with per-server field names.

📌 PR #106 review fixes — Two changes from Copilot PR review: (1) Renamed `RemoteMcpServerConfig` → `RemoteMcpSettings` across `types.ts`, `configuration.ts`, and `copilotService.ts` to avoid confusion with the SDK's re-exported `MCPRemoteServerConfig`. (2) Added explicit `{command, url}` rejection in `validateConfiguration()` — previously `isRemote = hasUrl && !hasCommand` silently treated ambiguous configs as local. Now errors early with actionable message. Removed redundant defense-in-depth guard in `buildMcpServersConfig()` since validation catches this upstream. Updated test to reflect the architecture: validation rejects, mapper trusts clean input. Key learnings: (1) Our local config types should use clearly distinct names from SDK re-exports — `RemoteMcpSettings` (ours) vs `MCPRemoteServerConfig` (SDK). (2) Prefer validation-layer rejection over mapper-layer guards — validation gives actionable errors, mapper-layer skipping is silent. (3) TypeScript narrowing with compound `in` checks (`'url' in x && !('command' in x)`) doesn't narrow union types properly in else branches — simpler `if ('url' in x)` works because the `command?: never` discriminant enables proper narrowing.

📌 Completed #100 — Markdown rendering in chat responses (PR #109). Added marked library (bundled via esbuild) to render AI assistant messages as formatted HTML instead of raw markdown text. Changes: (1) Installed `marked` as a dependency. (2) Added second esbuild entry point in `esbuild.config.mjs` to bundle `media/chat.js` → `dist/chat.js` with platform:browser/format:iife, bundling marked inline. (3) Updated `media/chat.js`: import marked, configure with `breaks: true` + `gfm: true`, render assistant messages via `marked.parse()` with `markdown-body` CSS class, accumulate raw markdown in `currentAssistantRawText` during streaming and re-render full text on each delta for correct incremental markdown display. (4) Added 150 lines of CSS in `media/chat.css` for `.markdown-body` elements — headers (h1-h6 with sizing), code blocks (monospace with theme background/border), inline code, blockquotes (left border), lists (ul/ol), tables (bordered), links (theme link colors), bold, hr, images — all using VS Code theme CSS variables. (5) Updated `src/extension.ts` to point webview `<script>` at `dist/chat.js`. Build clean, all 192 tests pass. Key learnings: (1) Webview JS files aren't bundled by the main esbuild — need a separate browser-targeted build step. (2) `marked.parse()` returns synchronous HTML by default — safe for streaming re-render on each delta. (3) Using `innerHTML` with marked output instead of `textContent` — marked handles HTML entity escaping internally. (4) `currentAssistantRawText` accumulator is essential — re-parsing full text on each delta ensures multi-token markdown constructs (code fences, tables) render correctly as they arrive. (5) `streamEnd` should capture `currentAssistantRawText` for message history, not `textContent` from the rendered DOM (which loses markdown formatting for future re-render).

📌 Completed copy-button feature — Added "Copy" button to code blocks in chat messages. Changes: (1) Added `addCopyButtons(container)` function in `media/chat.js` — walks DOM for `<pre>` elements, injects a positioned copy button with inline SVG icon (CSP-safe, no external URLs). Uses `navigator.clipboard.writeText()` to copy `<code>` text content. Shows "Copied!" feedback for 2 seconds. Guards against duplicates by checking for existing `.code-copy-btn` class. (2) Called `addCopyButtons()` after every markdown render — both in `appendMessage()` for complete messages and `appendDelta()` for streaming. (3) Added CSS in `media/chat.css`: `.markdown-body pre` gets `position: relative` to anchor button; `.code-copy-btn` is `position: absolute; top: 4px; right: 4px` with `opacity: 0` by default, `0.7` on `pre:hover`, and `1.0` on button hover. Uses VS Code theme tokens (`--vscode-button-secondaryBackground`, `--vscode-button-secondaryForeground`, `--vscode-button-secondaryHoverBackground`). Key learnings: (1) Inline SVG is the right approach for webview buttons — CSP blocks external image URLs and data URIs may be restricted. (2) Re-render during streaming destroys and recreates DOM, so copy buttons must be re-injected after each `innerHTML` assignment — the duplicate guard prevents stacking. (3) The `pre:hover > .code-copy-btn` selector handles the show-on-hover pattern cleanly without JavaScript.

📌 XSS fix for PR #109 — Added DOMPurify sanitization to `media/chat.js`. Copilot flagged that `marked.parse()` output was assigned directly to `innerHTML` without sanitization, creating XSS risk. Fix: (1) Installed `dompurify` as a dependency. (2) Added `require("dompurify")` import in `media/chat.js`. (3) Wrapped `marked.parse()` in `DOMPurify.sanitize()` inside the `renderMarkdown()` function — this is the single choke-point for all markdown→HTML conversion (used by both `appendMessage` and `appendDelta`). Default DOMPurify config preserves all HTML elements needed for markdown rendering (code blocks, pre, tables, headings, lists) while stripping dangerous content. Copy button feature unaffected since `addCopyButtons()` runs after sanitized HTML is inserted. esbuild bundles DOMPurify into `dist/chat.js` automatically via the browser/IIFE webview build. All 192 tests pass, build and typecheck clean. Key learnings: (1) DOMPurify's default allowlist is generous enough for rendered markdown — no custom config needed. (2) Centralizing sanitization in `renderMarkdown()` means all current and future innerHTML assignments go through sanitization automatically.
📌 Completed #81 — Model selector dropdown in chat UI. Added `forge.copilot.models` array setting (defaults to `["gpt-4.1", "gpt-4o", "gpt-4o-mini"]`), `<select id="modelSelector">` dropdown in webview button row, and `modelChanged` message handler with confirmation dialog. Flow: user selects model → webview sends `modelChanged` → extension confirms → updates `forge.copilot.model` globally → destroys session → resets conversation → sends `modelSelected` back. If cancelled, dropdown reverts via `modelSelected` with current model. CSS uses VS Code dropdown theme variables (`--vscode-dropdown-*`) with `margin-left: auto` for right-alignment. Extension sends `modelsUpdated` and `modelSelected` on webview init. Updated 13 test files — added `models` to all `ExtensionConfig` objects, updated message type filters in 3 integration tests. PR #111 targeting dev. 192 tests pass. Key learnings: (1) `Record<string, string>` test mocks (setupProvider/setupValidSettings) must NOT include `models: string[]` — causes type error. The mock's `defaultValue` fallback handles these via `config.get()`. (2) `waitFor` thresholds in integration tests must account for all init messages — adding new messages (modelsUpdated, modelSelected) can cause `waitFor` to resolve before stream messages arrive, making filtered arrays empty. Fix by filtering inside `waitFor` too. (3) Cherry-picking across branches with divergent code (e.g., mode-selector) causes merge conflicts — cleaner to apply changes manually on a fresh branch.

📌 Completed #100 — Markdown rendering in chat UI. Integrated `marked` library (bundled inline via esbuild for air-gap compliance), added `marked.parse()` in `renderAssistantMessage()` with DOMPurify sanitization (`DOMPurify.sanitize(html)`). CSS: heading/code-block/list styling using VS Code theme tokens. Key learnings: (1) Library bundling via esbuild for offline rendering — no npm dependency at runtime. (2) Marked is BYOB (Bring Your Own Sanitizer) — must pair with DOMPurify. (3) Streaming markdown accumulates with each delta — `marked.parse()` must be called after final message is complete, not on partial content. Build clean, 192 tests pass. PR #109 targeting dev.

📌 Completed #108 — Chat/Agent/Plan mode selector in webview. Added `forge.copilot.mode` enum setting (`"chat" | "agent" | "plan"`, default `"chat"`) to package.json and `configuration.ts`. Created `<select id="modeSelector">` dropdown in webview UI next to model selector. Mode selection sends `modeChanged` message → extension updates setting → destroys session → resets conversation. Each mode is passed to SDK as `model.extensions.mode` in session creation (model-specific behavior). Added 13 mode-selector-specific tests: setting read, enum validation, session config wiring, UI state sync, mode persistence across conversation boundary. Key learnings: (1) Modes are informational to the model — backend behavior depends on model support for extensions property. (2) Mode switching mid-conversation loses context (by design, same as model switching). (3) Test coverage critical for mode switching flow — 13 tests cover happy path, cancel, setting persistence. Build clean, typecheck passes, 205 tests pass. PR #110 targeting dev.

📌 Team update (2026-03-01T23:45:00Z): Phase 4 feature batch complete — 3 agents shipped markdown rendering (#100, PR #109), model selector (#81, PR #111), and mode selector (#108, PR #110). All PRs targeting dev, 205 tests passing. Phase 4/5 issues triaged: #103→Palmer, #104→Fuchs, #105→Palmer, #69→Fuchs, #78→Blair, #77→Childs. Scoping comments posted. Decisions merged to decisions.md (model selector UI pattern, RemoteMcpSettings rename). Orchestration logs written to .squad/orchestration-log/. — coordinated by Scribe

📌 Completed model config consolidation (#81 simplification) — Removed `forge.copilot.model` (singular) setting, consolidating to single `forge.copilot.models` array. Active model defaults to `models[0]` and is persisted in `workspaceState` (`forge.selectedModel`) when user changes via dropdown. `_getActiveModel()` helper resolves selection: persisted value if still in models array, else `models[0]`. `copilotService.getOrCreateSession()` and `resumeConversation()` now accept `model` as explicit parameter instead of reading `config.model`. Updated 13 test files, added 3 new tests (195 total). Key learnings: (1) Runtime state (selected model) should live in `workspaceState`, not user-facing settings — simpler config surface for users. (2) When moving state from config to runtime, the service layer should accept it as a parameter rather than reading config directly — cleaner separation. (3) `_getActiveModel()` validates persisted selection against current models array — handles case where user removes a model from their config after selecting it. (4) Error-scenario tests' mock `ExtensionContext` needed `workspaceState` mock added since constructor now reads from it.

## Learnings

### 2026-02-27: Extension & Webview Review for Issue #112
**Context:** Performed domain-specific code review of extension.ts, configuration.ts, media/chat.js, media/chat.css for webview lifecycle, message handling, UI state, DOM manipulation, context attachments, streaming, activation/deactivation, and configuration correctness.

**Key Findings:**
1. **Webview state is not preserved across hide/show** — when user collapses/expands the Forge panel, the webview is recreated with blank HTML but `this._conversationMessages` persists in provider memory. Critical UX issue.
2. **No cancellation support during streaming** — once `session.send()` fires, there's no way to abort. Need to expose session.abort() to a webview "Stop" button.
3. **Event listener leak on code copy buttons** — `addCopyButtons()` is called on every `appendDelta()`, and while it checks for duplicate buttons, it doesn't prevent duplicate listeners if markdown re-parsing replaces the DOM.
4. **Webview messages posted when hidden accumulate in VS Code's queue** — when user re-shows panel, 100+ queued `streamDelta` messages flush at once, causing jank.
5. **No default case in message handler** — malformed webview messages silently fall through the if-else chain. Need `else` block with `console.warn()`.

**Patterns Learned:**
- **VS Code Webview Lifecycle:** `resolveWebviewView()` is called every time the panel is shown, not just on first activation. State must be explicitly restored from `workspaceState` or reconstructed.
- **Message Queue Behavior:** VS Code queues messages sent to hidden webviews. This is usually good (no lost messages), but can cause UI stutter on rapid-fire updates like streaming. Need to check `this._view?.visible` or buffer deltas client-side.
- **Event Listener Cleanup:** Incremental DOM updates (e.g., streaming markdown) can orphan event listeners. Use event delegation or explicit cleanup patterns (WeakMap, data attributes).
- **Permission Request Timeouts:** The SDK permission handler pattern (lines 708-757) is elegant but needs UI feedback when timeouts fire. Auto-deny is correct behavior, but user must be notified.

**Tools Validated:**
- `this._workspaceState` is already used for model selection (line 266, 416) and message caching (line 542-543) — extend this for full UI state restoration.
- `this._view?.visible` property exists (part of WebviewView API) — should check before posting non-critical updates.
- VS Code's message queue is part of the platform — cannot be disabled, must design around it.

**Recommendations for Future Work:**
1. Add webview state restoration in `resolveWebviewView()` — send cached messages to webview on show.
2. Implement cancellation: add "Stop" button to webview, store active session in provider, call `session.abort()`.
3. Replace per-button copy listeners with event delegation on `#chatMessages`.
4. Add `this._view?.visible` checks before posting authStatus, streamDelta (or buffer deltas).
5. Add default case in `_handleMessage()` with logging.
6. Add timeout feedback for tool confirmations.

**Cross-references:**
- Related to `.squad/decisions.md` lines 61-66 (manual testing todos T-1 through T-5) — several edge cases found here would be caught by those tests.
- Validates architecture decision from `.squad/decisions.md` lines 16-25 (WebviewViewProvider pattern, streaming via event listeners).


📌 Completed #112 (Blair domain review, 2nd pass) — Thorough code review of extension.ts, configuration.ts, media/chat.js, media/chat.css, and package.json for issue #112. Produced 15 findings (0 critical, 2 high, 7 medium, 4 low, 2 info). Key learnings: (1) `sendMessageWithContext` (code action commands) bypasses the webview message flow — the user never sees the prompt that was sent, only the response. Needs a synthetic user message posted to webview. (2) `postContextAttached` and other `_view?.webview.postMessage()` calls silently drop messages when the webview isn't resolved — context attachments made before opening the panel are lost. (3) `conversationResumed` handler in chat.js is missing half the state resets that `conversationReset` has (`isStreaming`, `currentAssistantRawText`, `pendingContext`, etc.) — resuming mid-stream locks the UI. (4) Status bar tooltip for API key auth says "Click to sign in with Azure CLI" — copy-paste bug from the Entra ID branch (line 229). (5) Tool confirmation timeout (2 min) auto-denies but never updates the webview card — user sees stale Approve/Reject buttons. (6) No `retainContextWhenHidden` and no `vscode.getState()`/`vscode.setState()` — webview state is lost on panel hide/show, though the extension-side `_conversationMessages` array survives.

📌 Completed #112 code review medium fixes — Applied 15 medium-priority fixes from code-review.md across extension.ts, configuration.ts, and media/chat.js. Changes: (M1) CSS.escape for querySelector interpolations — 3 locations. (M3) HTTPS endpoint URL validation in validateConfiguration. (M5) sessionId format/length validation in resume/delete handlers. (M8) CONTEXT_CHAR_BUDGET public constant replacing magic 8000. (M9) clipboard.writeText .catch() with "Copy failed" feedback. (M10/M11) OutputChannel "Forge" for auth status update failure logging. (M12) Named timeout constants (AUTH_POLL_INTERVAL_MS, SIGN_IN_RECHECK_MS, TOOL_PERMISSION_TIMEOUT_MS, COPY_FEEDBACK_MS, AUTH_BANNER_DISMISS_MS). (M13) retainContextWhenHidden via registerWebviewViewProvider options. (M14) Shared resetUIState() for conversationReset/conversationResumed. (M15) Debounced markdown re-render (50ms throttle on appendDelta, final render on streamEnd). (M16) Reset currentAssistantRawText on error. (M17) chatFocused selection truncation to CONTEXT_CHAR_BUDGET. (M18) toolTimeout webview message + card UI update on permission timeout. (M20) assistant.message event subscription for accumulatedContent validation. Build, typecheck, and all 215 tests pass. Key learnings: (1) `retainContextWhenHidden` is set via `registerWebviewViewProvider` options, NOT on `WebviewView.options` or `webview.options` — the type definition only includes it in the registration call. (2) Markdown throttling requires final render in streamEnd to avoid missing the last delta. (3) `CSS.escape()` is available in webview (browser context) for safe querySelector interpolation.

📌 Completed #112 code review medium fixes — Applied 15 medium-priority fixes from code-review.md across extension.ts, configuration.ts, and media/chat.js. Changes: (M1) CSS.escape for querySelector interpolations. (M3) HTTPS endpoint URL validation. (M5) sessionId format/length validation. (M8) CONTEXT_CHAR_BUDGET public constant replacing magic 8000. (M9) clipboard.writeText .catch() handler. (M10/M11) OutputChannel logging for auth status update failures. (M12) Named timeout constants (AUTH_POLL_INTERVAL_MS, SIGN_IN_RECHECK_MS, TOOL_PERMISSION_TIMEOUT_MS, COPY_FEEDBACK_MS, AUTH_BANNER_DISMISS_MS). (M13) retainContextWhenHidden on WebviewView. (M14) Shared resetUIState() for conversationReset/conversationResumed. (M15) Debounced markdown re-render (50ms throttle on appendDelta). (M16) Reset currentAssistantRawText on error. (M17) chatFocused selection truncation. (M18) toolTimeout webview message + card update. (M20) assistant.message event subscription for content validation. Build, typecheck, and all 201 tests pass. Key learnings: (1) `retainContextWhenHidden` is on `WebviewView.options`, NOT `webview.options` — different API surface. (2) Markdown throttling requires final render in streamEnd to avoid missing the last delta. (3) `CSS.escape()` is available in webview (browser context) and handles special chars in data-attribute selectors.

📌 Created monochrome activity bar icon — Replaced placeholder shield+keyhole SVG in `resources/icon.svg` with a proper forge icon: anvil (top plate with horn, tapered body, wide base) + hammer (handle at 45° with perpendicular head) + 3 spark diamonds radiating from impact point. Uses `fill="currentColor"` for VS Code theme compatibility, `viewBox="0 0 24 24"` for standard sizing. All paths are flat/filled for readability at 16x16. Referenced in `package.json:29` under `viewsContainers.activitybar[0].icon`. Key learnings: (1) VS Code activity bar icons MUST be monochrome — the editor overrides colors with theme. Use `currentColor` not explicit color values. (2) Filled shapes (`fill`) are more visible at small sizes than stroked shapes (`stroke`). (3) At 16x16, each SVG unit in a 24-unit viewBox is ~0.67px — keep details ≥1.5 units for visibility.

📌 Changed default models from `["gpt-4.1", "gpt-4o", "gpt-4o-mini"]` to `[]` in package.json and src/configuration.ts. Updated description to mention values must match Azure AI Foundry model deployment name (not model name). Updated one test assertion in configuration.test.ts. Rationale: hardcoded model names don't match user deployments — empty default forces explicit configuration, avoiding confusing "model not found" errors on first run.

📌 Team update (2026-03-02T15:11:14Z): Models config consolidated — changed `forge.copilot.models` default to empty array. Fuchs repositioned README value prop from "air-gapped" to "organizational control". Both decisions merged to decisions.md. — decided by Fuchs & Blair
📌 Updated endpoint example URL — Changed user-facing example endpoint from `openai.azure.com` to `services.ai.azure.com` (Azure AI Foundry generic format) in two places: `package.json` settings description (line 98) and `src/configuration.ts` validation error message (line 76). No logic changes — the `isAzure` regex `/\.azure\.com/i` matches both old and new formats. Build, typecheck, and all 225 tests pass. Key learnings: (1) Example URLs in user-facing text should track Azure's current branding — `services.ai.azure.com` is the current Azure AI Foundry endpoint format. (2) The `isAzure` check is intentionally broad (`.azure.com`) to support multiple Azure endpoint formats without needing updates.

📌 Completed #120 Option A (squad/120-welcome-inline) — Implemented inline welcome card for first-run experience. P0 foundation: (1) added `models` validation to `validateConfiguration()` after apiKey check, (2) changed `_getActiveModel` fallback from `"gpt-4.1"` to `""` (safe empty string; validation gate handles the empty case before send flow), (3) updated `modelsUpdated` handler in chat.js to show disabled "No models configured" option when array is empty. Option A unique part: `resolveWebviewView` now sends a `configStatus` message with `hasEndpoint`, `hasAuth`, `hasModels` booleans; chat.js handles it with a new `renderWelcomeCard()` function that inserts a styled `.welcome-card` div at the top of `#chatMessages` showing a 🔨 welcome title, description, and config checklist with ✅/❌ indicators and "Configure" buttons. Card auto-dismisses when all three are configured. CSS uses `--vscode-textBlockQuote-background` and `--vscode-textBlockQuote-border` for theme-compatible warm styling. Key learnings: (1) When adding new lifecycle messages (like `configStatus`) to `resolveWebviewView`, tests that filter out lifecycle messages by type AND count total messages for `waitFor` timing conditions must be updated — the count check must use the filtered set, not the raw total, otherwise timing races occur. (2) Test settings objects that use `Record<string, string>` must become `Record<string, unknown>` when adding non-string fields like `models: string[]`. (3) `configStatus` is sent after `authStatus`, `modelsUpdated`, `modelSelected` — this ordering is intentional (auth status is always first so auth banner renders before the welcome card).

📌 Completed #120 Option B (squad/120-welcome-replace) — Implemented replace-area welcome screen for first-run experience. P0 foundation same as Option A: models validation in `validateConfiguration()`, `_getActiveModel` fallback to `""`, and empty-models UI in `modelsUpdated` handler. Option B unique part: added `#welcomeScreen` div BEFORE `#chatMessages` in the HTML template; `resolveWebviewView` sends `configStatus` message; chat.js `applyConfigStatus()` function toggles `.hidden` on `#welcomeScreen`, `#chatMessages`, and `.input-area` based on `hasEndpoint && hasAuth && hasModels`; `renderWelcomeScreen()` + `createSetupStep()` build the full-page setup wizard with 🔨 icon, title, subtitle, 3 step cards (each with ✅ or numbered circle indicator + action buttons), and "Need help?" docs link. `conversationReset` now guards `chatMessages.innerHTML = ""` behind `configIsComplete` flag. CSS adds global `.hidden { display: none !important }` utility class plus full `.welcome-screen` hierarchy of styles. Key learnings: (1) The `waitFor` count threshold in tests must be updated when new lifecycle messages are added to `resolveWebviewView` — `>= 4` with 4 init messages meant the test satisfied early before any stream events arrived. Updated to `>= 8` (4 init + 4 stream messages). (2) Option B and Option A share identical P0 foundation changes — when merging one, the other's P0 changes can be skipped. (3) `.input-area` has no ID — querySelector by class is fine since it's unique in the DOM.
📌 Team update (2026-03-02T17:42:36Z): Welcome UI mockups merged (inline and replace options documented) — decided by Scribe
