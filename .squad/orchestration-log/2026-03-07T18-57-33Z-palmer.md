# Orchestration Log: Palmer (DevOps)

**Timestamp:** 2026-03-07T18:57:33Z  
**Agent:** Palmer (DevOps Specialist)  
**Status:** ✅ COMPLETED

## Task

Remove marketplace publishing steps from `insider-release.yml` to prevent pre-release builds from being published to VS Code Marketplace.

## Outcome

**Result:** SUCCESS

### Changes Made

**File:** `.github/workflows/insider-release.yml`

Removed:
- "Compute marketplace version" step (was computing `{major}.{minor}.{run_number}` versions)
- "Publish to VS Code Marketplace (Pre-Release)" step (was running `vsce publish --pre-release`)
- `VSCE_PAT: ${{ secrets.ADO_MARKETPLACE_PAT }}` secret reference

**Final State:** Workflow now ends at "Verify release" step, confirming GitHub Release creation with `.vsix` artifact.

### Distribution Path (New)

| Release Type | Distribution | Versioning | Trigger |
|-------------|-------------|-----------|---------|
| **Insider** | GitHub Releases only | `{version}-insider+{short-sha}` | `git push origin` → `insider-release.yml` |
| **Stable** | GitHub Releases + Marketplace | semver (package.json) | Merge to `main` → `release.yml` |

## Decision

Merged to `.squad/decisions.md`:
- `Decision: Insider Release — Marketplace Publishing Removed` (Palmer, 2026-03-06)

## Cross-Agent Impact

None identified. This is DevOps-only infrastructure change.

## Next Steps

None. Workflow is operational and tested.
