# Orchestration Log: Childs (SDK Dev)

**Timestamp:** 2026-03-01T21:52:00Z  
**Agent:** Childs (SDK Dev)  
**Mode:** background  
**Status:** SUCCESS

## Work Completed

1. Added `buildMcpServersConfig()` function to `src/copilotService.ts`
   - Maps extension `McpServerConfig` to SDK `MCPLocalServerConfig`
   - Sets `type: "local"`, `tools: ["*"]` for each server
   - Returns `Record<string, MCPLocalServerConfig>`

2. Wired MCP config into `getOrCreateSession()`
   - Reads config via `readMcpServerConfig()`
   - Passes `mcpServers` to SDK `SessionConfig`
   - Handles no-servers case gracefully

3. Wired MCP config into `resumeConversation()`
   - Passes built config to `client.resumeSession()`
   - Consistent with getOrCreateSession logic

4. Updated SDK types: re-exported `MCPLocalServerConfig`

## Outcomes

- MCP servers are passed to Copilot SDK on session creation
- Both new and resumed conversations respect MCP config
- SDK receives typed `Record<string, MCPLocalServerConfig>`
- Air-gap constraint: local-only (type: "local"), no remote MCP
