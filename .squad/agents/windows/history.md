# Project Context

- **Owner:** Rob Pitcher
- **Project:** Enclave — VS Code extension providing Copilot Chat for air-gapped environments. Uses @github/copilot-sdk in BYOK mode to route inference to private Azure AI Foundry endpoint. No GitHub auth, no internet.
- **Stack:** TypeScript, VS Code Extension API (Chat Participant), @github/copilot-sdk, esbuild, Azure AI Foundry
- **Created:** 2026-02-27

## Key Files

- `src/extension.ts` — Request handler with error paths to test
- `src/copilotService.ts` — SDK client lifecycle with error handling to test
- `src/configuration.ts` — Configuration validation to test
- `specs/PRD-airgapped-copilot-vscode-extension.md` — Success criteria SC1-SC7
- `package.json` — Test scripts

## Learnings

<!-- Append new learnings below. Each entry is something lasting about the project. -->

📌 Team update (2026-02-27T04:19:00Z): 28 GitHub issues (#2–#29) created and linked to project board #8 (Enclave). PRD fully decomposed with dependencies, priorities, milestones, and squad routing. Ready for pickup. — decided by MacReady & Childs. Project: https://github.com/robpitcher/enclave/projects/8

📌 Team update (2026-02-27T05:03:36Z): MVP triage complete. Build & packaging issues #15–16 routed to @copilot for validation scripts; test management remains with Windows. All testing issues (#9–10, #13) blocked on #2 completion. — decided by MacReady

✅ Build pipeline validation (2026-02-27T05:48:00Z): Issue #2 validated and closed. `npm install`, `npm run build`, and `npm run package` all succeed. Project structure matches PRD Section 8. esbuild produces valid 184KB bundle with @github/copilot-sdk correctly bundled. `.vsix` packaging works but produces 95MB file due to missing `.vscodeignore` (includes node_modules, .squad, specs). Core build pipeline is sound—bloat is a packaging config issue. Expected final size ~500KB once `.vscodeignore` is added. — validated by Windows

✅ Error scenario tests (SC5) completed: Issue #13 — added 10 tests in `src/test/error-scenarios.test.ts` covering all 7 error paths: missing config validation, CopilotCliNotFoundError (ENOENT/not found/cannot find), session.error event, general startup error, sendMessage rejection, config validation display with settings button, and session removal on error. All 26 tests pass (10 new + 16 existing). PR #43 opened targeting dev. Note: `multi-turn.test.ts` has 6 pre-existing failures unrelated to this work. — validated by Windows

✅ WebviewView test infrastructure rewrite (Issue #51): Rewrote all 3 test files (extension.test.ts, multi-turn.test.ts, error-scenarios.test.ts) for WebviewView message protocol. Created shared test helpers in `src/test/webview-test-helpers.ts`. Updated vscode mock with `window.registerWebviewViewProvider` and `Uri` class. 50 total tests: 30 pass on this branch (copilotService + configuration + direct session tests), 20 expected failures (extension.ts not yet rewritten). All 50 pass on `squad/53-webview-sidebar` where Blair's WebviewView implementation lives. PR #55 opened targeting dev. Depends on #53 merge first. Key protocol tested: streamStart→streamDelta→streamEnd, error posting, conversationReset, sendMessage/newConversation commands. — validated by Windows

📌 Team update (2026-02-27T15:30:00Z): WebviewView migration kickoff. Issues #51 (Windows) and #53 (Blair) launched on parallel branches. Rob confirmed: no Chat Participant API (requires GitHub auth); use WebviewView sidebar instead. Architecture decision merged to decisions.md — Option 1 (WebviewView sidebar) recommended. Backend (copilotService, configuration) unchanged; only extension.ts and webview assets needed. Phase 1 estimate: 2-3 days for minimal viable sidebar + tests. — orchestrated by Scribe, team ready for parallel work on test scaffolding and implementation

📌 Team update (2026-02-27T14:39:00Z): Round 3 complete. Windows delivered air-gap validation tests (18 tests, #47); MacReady delivered installation and configuration docs (#45); Blair delivered session cleanup implementation (PR #46). All 59 tests passing. All PRs targeting dev. Decisions merged to `.squad/decisions.md`. Orchestration logs written. — Scribe

🔧 Rebase conflict resolution (2026-02-27T18:33:00Z): PR #57 (`squad/14-streaming-tests` → dev) had merge conflicts after PRs #56, #58, #59 merged into dev. Rebased onto origin/dev. Single conflict in README.md resolved by keeping dev's version (docs updates from #58). Discovered that the original `fix: address PR #56 review comments` commit had accidentally emptied `air-gap-validation.test.ts` (327 lines → 0). Restored from origin/dev and added a fix commit. All 67 tests pass across 6 suites. Force-pushed, PR now `mergeable_state: clean`. **Lesson:** When branches carry unstaged/stashed files from other work streams, rebase conflicts can silently wipe file contents — always verify file sizes post-rebase. — validated by Windows

✅ Air-gap validation tests (Issue #11): Created `src/test/air-gap-validation.test.ts` with 17 tests validating SC2 (BYOK mode) and SC3 (no GitHub auth). Tests confirm: (1) CopilotClient and sessions work without GITHUB_TOKEN env var, (2) provider config uses only user-configured Azure endpoint (never github.com), (3) provider type set to `azure` with `apiVersion: 2024-10-21` for Azure endpoints, (4) session config contains zero GitHub references, (5) supports both Azure and generic OpenAI-compatible endpoints. All 67 tests pass. PR #59 opened targeting dev. Key learning: Use unique conversation IDs per test to avoid session caching issues across test runs. — validated by Windows

📌 Team update (2026-02-27T17:00:00Z): MVP batch completion. Air-gap validation (17 tests, #59) and streaming smoothness (16 tests, #57) completed in parallel with MacReady (#52 docs) and Blair (#56 packaging). 33 tests written total. All PRs targeting dev. Session logs written to .squad/orchestration-log/ and .squad/log/. — completed by Windows, Scribe orchestration

✅ Tool approval flow tests (Issue #25): Created `src/test/tool-approval.test.ts` with 13 tests (4 passing now, 9 `.todo()` awaiting implementation). Passing tests: session config shape validation, text-only chat regression, autoApproveTools defaults to false, autoApproveTools reads from config. Todo tests cover: permission request → webview toolConfirmation, auto-approve bypass, SDK confirmation/rejection on user response, toolResult rendering (success/error), unknown toolCallId handling, concurrent tool call tracking, and availableTools removal. All 80 tests across 7 suites pass (71 active + 9 todo). SDK tool event types confirmed: `tool.execution_complete` has `success`, `result`, `error` fields; `PermissionRequest` has `kind`, `toolCallId`; `PermissionRequestResult` uses `approved`/`denied-interactively-by-user`. Key pattern: `onPermissionRequest` callback in `SessionConfig` is the SDK hook for tool approval. — validated by Windows

📌 Team update (2026-02-28T0000Z): Tool approval flow uses callback + events pattern. Test framework ready; 4/13 tests active. Priority: implement remaining 9 tests for shell, write, read, mcp, url permissions. — decided by Childs & Windows

✅ Context attachment tests (Issue #26): Created `src/test/context-attachment.test.ts` with 7 tests (3 active, 4 `.todo()`). Active tests: (1) plain text message sends only prompt text — no context prefix (regression), (2) empty context array treated as no context, (3) streaming works correctly for plain messages. Todo tests awaiting Blair/Childs implementation: context included in prompt, multiple context items, context truncation >8000 chars, command registration (forge.attachSelection/forge.attachFile). Confirmed `_buildPromptWithContext` does NOT exist yet — all context-dependent tests are `.todo()` with detailed assertion comments for when the feature lands. All 87 tests pass (78 active + 9 todo) across 8 suites. — validated by Windows

✅ DefaultAzureCredential auth tests (Issue #27): Created `src/test/auth/credentialProvider.test.ts` (13 tests) covering both auth paths. ApiKeyCredentialProvider: returns stored key, handles empty keys. EntraIdCredentialProvider: verifies cognitive services scope, returns raw token string (not AccessToken object), surfaces AggregateAuthenticationError and CredentialUnavailableError, calls getToken fresh each time. Factory: returns correct provider type per authMethod, reads from SecretStorage for apiKey mode. Updated `src/test/copilotService.test.ts` (+3 tests): bearerToken on ProviderConfig for entraId, apiKey for apiKey mode, Azure provider config with entraId. Updated `src/test/configuration.test.ts` (+7 tests): validates apiKey skip for entraId, requires apiKey for apiKey mode, default authMethod is "entraId", reads configured authMethod. Fixed all existing tests across 6 files to pass new `authToken` parameter and `authMethod` on ExtensionConfig. All 38 auth-related tests pass. Pre-existing WebviewView failures (24 tests in 5 files) unrelated. Key learning: `EntraIdCredentialProvider` constructor takes a credential object (not zero-arg) — mock `@azure/identity` with a class, not a factory function, because the implementation calls `new DefaultAzureCredential()`. `createCredentialProvider` is async (returns `Promise<CredentialProvider>`) due to dynamic `import("@azure/identity")`. — validated by Windows

📌 Team update (2026-02-28T16:16Z): Design review ceremony for #27 complete. 10 architecture decisions merged into decisions.md. Childs owns credentialProvider.ts (interface + impls); Blair owns authMethod setting + config; Windows (✓ complete) owns test coverage (23 new tests for both auth paths, all 81 tests pass, 24 pre-existing failures unrelated). Implementation coordinated via contracts and signatures. Ready for user review before merge to dev. — decided by MacReady

✅ Auth status provider tests (Issue #80): Created `src/test/auth/authStatusProvider.test.ts` with 15 tests covering `checkAuthStatus()` function behavior. Tests validate all 5 documented outcomes: (1) no endpoint configured → notAuthenticated, (2) Entra ID success → authenticated with method "entraId", (3) Entra ID failure → error with message, (4) API key exists → authenticated with method "apiKey", (5) API key missing/empty/whitespace → notAuthenticated. Added edge cases: undefined endpoint, non-Error rejections, type safety validation for discriminated union fields. Key pattern: function never throws — always returns AuthStatus object. All 15 tests pass. Follows established test structure from credentialProvider.test.ts: mock @azure/identity at module level with CLASS (not factory fn), mock SecretStorage with get/store/delete/onDidChange. Test file path: `src/test/auth/authStatusProvider.test.ts`. — validated by Windows

📌 Team update (2026-02-28T23:39:00Z): Issue #80 — Auth status UX complete. Windows wrote 15 unit tests for authStatusProvider.ts covering all outcomes: no endpoint, Entra ID success/failure, API key exists/missing. Tests validate discriminated union fields, error messages, never-throw contract. Childs created provider module; Blair wired UI. Decision merged to decisions.md. — coordinated by all three

---
📌 Team update (2026-03-01T03:14:32Z): Auth system improvements — Childs added 4 new error classification tests. Pre-existing `error-scenarios.test.ts` failure unrelated (authMethod from #27). All authStatusProvider tests (17 total) pass. — decided by Childs & Blair

📌 Team update (2026-03-01T12:20:19Z): Context chips in sent messages — chat history now displays visual badges (📎/📄) showing which files/selections were attached to each user message. Blair's UI enhancement improves conversation review experience. 122 tests pass. — decided by Blair
✅ Tool control settings tests (Issue #91): Created `src/test/tool-control-settings.test.ts` with 19 tests covering `forge.excludedTools` and `forge.availableTools` settings. Tests validate: (1) default excludedTools is `["url"]` via getConfiguration and SessionConfig, (2) custom excludedTools passed through to createSession, (3) availableTools passed when configured, omitted when not, (4) console.warn fired when both settings set simultaneously, (5) empty/undefined arrays don't crash session creation, (6) new conversations pick up changed settings, cached sessions don't. Implementation already landed in copilotService.ts and configuration.ts — all 19 tests pass. Key pattern: `stopClient()` in BOTH beforeEach and afterEach is critical for cross-file test isolation due to copilotService's module-level `client`/`sessions` state. All 148 tests pass across 11 suites (5 todo from other files). — validated by Windows

✅ Conversation history persistence tests (Issue #87): Created `src/test/conversation-history.test.ts` with 33 tests covering 4 new service functions from Childs: listConversations, resumeConversation, getLastConversationId, deleteConversation. Extended `src/test/__mocks__/copilot-sdk.ts` with 4 mock methods (listSessions, resumeSession, getLastSessionId, deleteSession) matching SDK interface. Tests validate: (1) ConversationMetadata mapping from SDK SessionMetadata, (2) resumeSession passes full SessionConfig (model, provider, streaming, excludedTools, onPermissionRequest), (3) provider config wiring (azure vs openai types, apiKey vs bearerToken auth), (4) tool exclusion settings applied to resumed sessions, (5) graceful empty-state handling (no sessions), (6) error paths (session not found, SDK connection errors, expired tokens). All 33 conversation-history tests pass. Note: Childs' implementation broke 5 pre-existing tests in other files (multi-turn, context-attachment, extension, tool-approval) — likely workspaceState persistence side effects from storing lastSessionId on every message. My mock changes (adding 4 SDK methods) are minimal and correct; failures are Childs' responsibility to fix. Key pattern: resumeConversation calls SDK's `client.resumeSession(sessionId, config)` with both sessionId AND full config object, not just sessionId alone. — validated by Windows

🔧 PR #102 review fix — dynamic imports masking regressions (2026-03-01): Converted conversation-history.test.ts from dynamic imports to static imports for listConversations, resumeConversation, getLastConversationId, deleteConversation. Removed 33 `if (!func) return` guard clauses that silently skipped assertions. Updated calls to listConversations(config), getLastConversationId(config), and deleteConversation(id, config) to match Childs' updated signatures. Net: -156/+27 lines. All 33 conversation-history tests pass. 2 pre-existing failures in copilotService.test.ts and air-gap-validation.test.ts are unrelated (sessionId in createSession config, and "github" substring in azure URL). **Lesson:** Dynamic imports with try/catch as a compatibility shim should be replaced with static imports once the contract is implemented — otherwise test suites become silent green lies. — validated by Windows

✅ MCP server configuration tests (Issue #90): Created `src/test/mcp-server-config.test.ts` with 13 tests covering MCP server config support across 3 layers. Configuration reading (3 tests): undefined when unconfigured, single server, multiple servers with args/env. Validation (6 tests): valid config, undefined mcpServers, empty object, missing command, empty command string, multiple invalid servers. SDK passthrough (4 tests): mcpServers passed to createSession, omitted when undefined, MCPLocalServerConfig mapping verified (type=local, tools=["*"], args defaults to [], env omitted when not set), resumeConversation passes mcpServers to resumeSession. Also added `McpServerConfig` interface to types.ts, `mcpServers` field to ExtensionConfig, validation in `validateConfiguration()`, and `buildMcpServersConfig()` to copilotService.ts. All 182 tests pass (179 active + 3 todo) across 13 suites. Key pattern: `buildMcpServersConfig()` returns `Record<string, MCPLocalServerConfig> | undefined` — only spread into session config when defined. `MCPLocalServerConfig` re-exported from SDK. — validated by Windows

🔧 PR #106 review — 3 test additions for MCP config realism and ambiguity (2026-03-01): Added 3 tests to `src/test/mcp-server-config.test.ts`. (1) Empty mcpServers object test: verifies `getConfiguration()` preserves `{}` when VS Code returns its contributed default — aligns mock with real VS Code behavior where package.json default is `{}`, not `undefined`. (2) Validation ambiguity test: verifies `validateConfiguration()` produces an error when a server entry has both `command` and `url` — catches misconfiguration that would silently pick one transport. (3) Service-layer defense-in-depth test: verifies `buildMcpServersConfig()` skips entries with both `command` and `url` so ambiguous entries never reach the SDK. Tests await companion implementation changes before passing. **Lesson:** Test mocks should match VS Code's actual contributed defaults from package.json, not idealized undefined values. — validated by Windows

✅ Test coverage gap analysis (Issue #112, 2026-03-02): Full analysis of 195 passing tests across 13 test suites against 7 source files (~1855 lines). Found 22 findings: 2 Critical, 5 High, 8 Medium, 4 Low, 3 Info. **Critical gaps:** (1) `codeActionProvider.ts` has ZERO tests (49 lines), (2) conversation management handlers (`_handleListConversations`, `_handleResumeConversation`, `_handleDeleteConversation`) completely untested. **High gaps:** `_rewriteAuthError()`, `updateAuthStatus()` status bar mapping, `modelChanged` handler, `postAuthStatus()` dedup logic, `sendFromEditor()` + code action commands. **Strengths:** Service layer (copilotService, configuration, auth) rated Good — copilot-sdk mock is accurate with no drift, test isolation via stopClient() is solid, no flaky patterns detected (all async uses vi.waitFor, not setTimeout). Mock accuracy confirmed: 7 SDK methods match real CopilotClient interface. Full findings in `.squad/reviews/windows-findings.md`. **Key learnings:** (1) extension.ts at 905 lines is the coverage bottleneck — 17 untested methods/paths despite 5 test files touching it. (2) `getSessionCount()` in copilotService.ts is dead code (defined but never exported or called). (3) 3 todo tests in tool-approval.test.ts await implementation but the source code for all 3 cases already exists. — validated by Windows

✅ Code review fix tests M23+M24 (Issue #112, 2026-03-02): Added 14 new tests across 2 files for medium-priority findings. **M23** (9 tests in `conversation-history.test.ts`): Webview handler tests for `_handleListConversations`, `_handleResumeConversation`, `_handleDeleteConversation` — tested via webview message protocol using new `simulateWebviewCommand` helper. Covers: listConversations posts conversationList, error handling, resumeConversation with valid/empty/missing sessionId, SDK errors during resume, deleteConversation with refresh, deletion errors, empty sessionId guard. **M24** (5 tests in `error-scenarios.test.ts`): `_rewriteAuthError` tested via sendMessage error path. Covers: 401, unauthorized, authorization, /login keywords all rewritten to "API key is missing or invalid" message; non-auth errors passed through unchanged. **Infra fixes:** Added `createOutputChannel` to vscode mock (extension.ts now calls it on activate). Added `simulateWebviewCommand` to `webview-test-helpers.ts`. Added `authStatusProvider` + `credentialProvider` mocks to conversation-history.test.ts using `vi.hoisted()` for mock factory hoisting. Used `vi.hoisted()` pattern for the first time — needed when vi.mock factory references variables declared in module scope. All 218 tests pass (215 active + 3 todo), 14 test files, 0 failures. Previously had 4 pre-existing failures that are now fixed by the vscode mock update. — validated by Windows

🔧 PR #119 review fix — getText mock and empty selection (2026-03-02): Addressed 2 review comments on sendFromEditor.test.ts. (1) `getText` mock now uses `mockImplementation` with range-aware behavior: returns selection text when called with a range arg, returns `"FULL DOC: ..."` when called without — catches regressions where production code calls `getText()` instead of `getText(selection)`. Added `fullDocText` option to `createMockEditor` for customizing full-document return value. (2) Empty selection test now uses non-empty document (`fullDocText: "const x = 1;\nconst y = 2;"`) with zero-length selection (`startChar: 5, endChar: 5`), properly simulating "cursor in document but nothing selected" — previously used empty document which didn't test the real scenario. All 225 tests pass, type checks clean. **Lesson:** `mockReturnValue` is insufficient for `getText` — always use `mockImplementation` with argument discrimination when the production code passes different args in different paths. This pattern catches call-site regressions that a constant mock silently hides. — validated by Windows

🔧 CI lint fix (Issue #104, 2026-03-02): Fixed 2 lint issues blocking CI on `squad/104-ci-test-workflow`. (1) Removed unused `import type { AuthStatus }` from `authStatusProvider.test.ts` — the type was imported but never referenced (ESLint error: `@typescript-eslint/no-unused-vars`). (2) Removed stale `// eslint-disable-next-line @typescript-eslint/no-explicit-any` from `codeActionProvider.test.ts` — the directive was unnecessary since no `no-explicit-any` violation existed on the next line (ESLint warning: unused directive). Lint now passes with 0 errors. **Lesson:** When importing types purely for documentation or readability, either use them in assertions/casts or don't import them — ESLint strict mode treats unused type imports the same as unused value imports. Also: eslint-disable directives can go stale when the underlying rule config or code changes — periodically run `--fix` to clean them up. — validated by Windows

✅ Welcome screen & API key clearing tests (Blair's fixes, 2026-03-02): Added 7 new tests to `src/test/extension.test.ts` covering Blair's welcome screen auto-detection and clear API key features. **Welcome screen tests (2):** (1) `postConfigStatus` posts correct configStatus message shape to webview with hasEndpoint/hasAuth/hasModels flags, (2) `_sendConfigStatus` still called during initial resolveWebviewView (regression guard). **Clear API key tests (5):** (1) clearApiKey webview command calls `secrets.delete("forge.copilot.apiKey")`, (2) shows confirmation message via `showInformationMessage`, (3) triggers auth refresh callback, (4) openSettings quick pick includes "Clear API Key" option, (5) selecting "Clear API Key" from quick pick calls delete. All 17 tests in extension.test.ts pass. Key patterns: (1) Use `simulateWebviewCommand` helper for webview message protocol tests. (2) Test auth refresh via callback spy pattern, not authStatus message count (dedup logic prevents duplicate messages). (3) QuickPick items can be captured via `mockImplementation` callback for assertion without triggering selection. Implementation already landed in extension.ts — `_clearApiKey()` private method, `openSettings()` quick pick option, `clearApiKey` command in `_handleMessage`, `postConfigStatus()` public method called from `updateAuthStatus()` after `postAuthStatus()`. — validated by Windows

📌 Team update (2026-03-02T22:30:00Z): Welcome Screen State Management Pattern — postConfigStatus and configStatus synchronization finalized — decided by Blair + Windows (7 new tests added, all 232 tests pass)

🔧 Unsafe mock cast fix (2026-03-03): Fixed 3 instances of unsafe casts `(execSync as unknown as ReturnType<typeof vi.fn>)` in `src/test/auth/credentialProvider.test.ts` (lines 142, 166, 190) by replacing with idiomatic `vi.mocked(execSync)`. `copilotService.test.ts` already uses the correct pattern (lines 254, 330). All 246 tests pass, typecheck clean. **Lesson:** `vi.mocked()` is the Vitest idiomatic way to access mocked module exports — it provides proper type inference and makes the intent clear that this is a Vitest mock, not a generic function cast through unknown. — validated by Windows

✅ CLI auto-installer tests (requested by Rob): Created `src/test/cliInstaller.test.ts` with 30 tests covering the `installCopilotCli()`, `getManagedCliPath()`, and `isManagedCliInstalled()` interfaces. Tests written before implementation (TDD style) based on interface contract from spec. Happy paths: npm install success, HTTP tarball fallback when npm missing, globalStoragePath creation, SDK version default. Error paths: both methods failing, npm timeout, HTTP 404/network errors, corrupted downloads, permission denied on storage path. Resolution chain tests document priority: configured cliPath > managed copy > PATH discovery. Startup probe tests document incompatible binary rejection (unknown option --headless). Concurrency test validates parallel install attempts don't corrupt state. Actionable error message tests validate user guidance. Key test patterns: mock child_process.execFile, fs.access, fs.createWriteStream, https.get with vi.hoisted() for module-level variables; use dynamic import after mocking to pick up mocks; mock stream events with .on() handlers; test both callback-style and promise-style async patterns. File: 600 lines, 30 tests across 9 describe blocks. Test coverage includes all 19 test cases from spec. Awaiting parallel implementation by Childs (cliInstaller.ts) and Blair (copilotService.ts integration). — validated by Windows

## 2026-03-03T20-55-00Z: CLI Installer Test Suite

Wrote `cliInstaller.test.ts` with 18 unit tests covering npm success, HTTP tarball fallback, error cases, CLI compatibility probing, and platform-specific binaries. Initial tests required rework by coordinator to match actual spawn() behavior. Final: 18 tests passing, 269 total suite passing.

**Outcome:** ✅ SUCCESS (with coordinator rework) — Full coverage of install paths.

## 2026-03-04: Az CLI Detection Tests (forge.signIn)

Wrote `src/test/az-cli-detection.test.ts` with 12 tests covering Blair's `isAzCliAvailable()` function and its integration in the `forge.signIn` command handler. Tests exercise the function indirectly through the registered command since it's module-private.

**Coverage:**
- **az found (entraId):** terminal created with `az login`, no error shown (2 tests)
- **az NOT found (entraId):** error message with "Azure CLI is required" + install URL, no terminal created, openExternal on button click, no-op on dismiss (5 tests)
- **Cross-platform:** `which` on Linux/macOS, `where.exe` on Windows (3 tests)
- **apiKey regression:** no az detection, no terminal (2 tests)

**Mock additions:** Added `showErrorMessage`, `showWarningMessage`, `env.openExternal`, and `Uri.parse` to `src/test/__mocks__/vscode.ts` — needed for these and future tests.

**Key pattern:** `isAzCliAvailable()` is tested indirectly by extracting the `forge.signIn` handler from `registerCommand` mock calls. The `makeAzNotFound()` helper selectively throws on `which`/`where.exe` + `az` args while passing through other `execFileSync` calls.

**Outcome:** ✅ SUCCESS — 12 new tests, 291 total passing (17 files), no regressions.
