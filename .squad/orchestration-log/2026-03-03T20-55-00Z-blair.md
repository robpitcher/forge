# Orchestration Log: Blair (Extension Dev)
**Timestamp:** 2026-03-03T20-55-00Z  
**Agent:** Blair (Extension Dev, claude-sonnet-4.5)  
**Mode:** background

## Summary
Wired CLI auto-install flow into extension.ts with ask-first UX, progress notification, error handling, and globalStoragePath threading through copilotService.

## What Was Done

1. **Extended `extension.ts`** — added install dialog flow:
   - Catch `CopilotCliNeedsInstallError` in `_handleChatMessage` error handler
   - Show info dialog: "Forge needs the GitHub Copilot CLI to work. Install it now?"
   - Buttons: "Install", "Cancel"

2. **Progress notification integration:**
   - On Install button: `vscode.window.withProgress(ProgressLocation.Notification, ...)`
   - Passes `context.globalStorageUri.fsPath` to `installCopilotCli()`
   - Shows "Installing Copilot CLI..." during execution
   - Success message: "Copilot CLI installed successfully. Try sending a message again."
   - Error message with "Open Settings" button for fallback configuration

3. **GlobalStoragePath threading:**
   - Modified `_getOrCreateSession()` to accept and pass `globalStorageUri.fsPath` to copilotService
   - Updated all session-related calls (`getOrCreateSession`, `resumeConversation`, `listConversations`, etc.)
   - Stored as `_globalStoragePath` in extension context

4. **Error handler updates:**
   - Reuses existing `openSettings()` command for fallback path after install failure
   - Cancel button shows manual install instructions
   - All error messages are actionable (tell user what to do)

5. **Decision documented** — `.squad/decisions/inbox/blair-cli-install-ux.md` captures ask-first pattern rationale and flow

## Outcome
✅ **SUCCESS**  
- Extension ready for CLI auto-install UX
- Dialog respects user consent (no surprise installations)
- Progress notification doesn't block editor
- Error paths point user to Settings for manual config
- globalStoragePath correctly threaded through call chain

## Next
Windows writes integration tests (cliInstaller.test.ts) to validate install paths and error cases.
