# Orchestration Log — Blair (Extension Dev)

**Timestamp:** 2026-03-06T22:52:49Z  
**Task:** Fix `_rewriteAuthError()` to be auth-method-aware  
**Status:** SUCCESS

## Files Modified

- `src/extension.ts` (2 hunks)
  - Signature change: Added `authMethod: AuthMethod` parameter to `_rewriteAuthError()`
  - Entra ID branch: New conditional logic for `authMethod === "entraId"` with RBAC guidance message
  - API Key branch: Unchanged (existing message preserved)

## Outcome

- `_rewriteAuthError()` now differentiates error messages by auth method
- Entra ID users see: "Entra ID authentication was rejected by the endpoint. Ensure your account has the 'Cognitive Services OpenAI User' role on the Azure AI resource. Check Access control (IAM) in Azure Portal."
- API Key users continue to see API key guidance (unchanged)
- Decision documented in `.squad/decisions/inbox/blair-auth-error-rewrite.md`

## Technical Notes

- Parameter choice: Added as function parameter (not instance field) because `config.authMethod` is available at the single call site
- Consistent with existing pattern: `_extractEntraIdErrorSummary()` already differentiates errors by auth method
- Future extensibility: New auth methods will require new branches in `_rewriteAuthError()`
