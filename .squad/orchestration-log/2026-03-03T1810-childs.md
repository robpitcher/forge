# Childs (SDK Dev) — 2026-03-03T18:10Z

**Triggered by:** Rob Pitcher — address PR #129 review comments

## Work Completed

### 1. Fixed Command Injection in validateCopilotCli()
- **File:** `src/copilotService.ts`
- **Issue:** Used `execSync` with string interpolation for user-configurable CLI path
- **Fix:** Replaced with `execFileSync(cliPath, ["--version"], { timeout: 5000 })`
- **Rationale:** Shell bypass for user-input paths prevents command injection and quoting breakage on Windows

### 2. Added CLI Validation Functions
- **Exported functions:**
  - `validateCopilotCli(cliPath: string): Promise<CopilotCliValidationResult>`
  - `discoverAndValidateCli(configuredPath?: string): Promise<CopilotCliValidationResult>`
- **Validation logic:** Runs `--version` check to distinguish @github/copilot CLI from other copilot binaries
- **Error classification:** Returns `{ valid: true }` or `{ valid: false, reason: "not_found"|"wrong_binary"|"version_check_failed" }`
- **Type added:** `CopilotCliValidationResult` union type

### 3. Test Coverage
- Added 11 new tests to `src/test/copilotService.test.ts`
- All existing tests pass

## Decisions Made

1. **execFileSync for user-configurable paths:** Rule established for future code — any binary execution with user-config paths MUST use `execFileSync` + argv array, never `execSync` + shell string
2. **Timeout pattern:** Added 5-second timeout to CLI validation, 10-second timeout pattern for credential provider (coordinated with Blair)

## Status

✅ **Complete** — Code committed directly  
✅ Typecheck: `npx tsc --noEmit` passes  
✅ Tests: `npm test` — all 246 tests pass

## Dependencies

Delivered in parallel with Blair's extension activation logic (CLI preflight validation) and credentialProvider timeout fixes. Decisions recorded in `.squad/decisions/inbox/`.
