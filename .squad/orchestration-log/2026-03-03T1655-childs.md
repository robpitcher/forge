# Orchestration Log: Childs (SDK Dev)
**Date:** 2026-03-03T16:55Z  
**Task:** Auto-Recovery for Azure CLI "No Subscription" Errors  
**Mode:** background  
**Status:** ✅ SUCCESS

## Work Done
- Implemented auto-recovery logic in `EntraIdCredentialProvider.getToken()` to handle "No subscription found" errors
- Detection: Catches errors containing "No subscription found" or "az account set"
- Recovery path: Runs `az account list`, selects first enabled subscription, runs `az account set --subscription <id>`
- Retry logic: Re-attempts `DefaultAzureCredential.getToken()` once per provider instance
- Error messages: Actionable guidance if recovery fails

## Files Modified
- `src/auth/credentialProvider.ts` — added `hasAttemptedRecovery` flag, `isNoSubscriptionError()` helper, `autoSelectSubscription()` method
- `src/test/auth/credentialProvider.test.ts` — added 4 new tests covering recovery scenarios

## Test Results
- ✅ All 17 existing credential tests pass
- ✅ 4 new tests added (recovery happy path, once-per-instance, no subscriptions edge case, non-subscription error passthrough)
- ✅ Full suite: 236 tests passing
- ✅ TypeScript typecheck: `npx tsc --noEmit`

## Decision Logged
- **ID:** Auto-Recovery for Azure CLI "No Subscription" Errors
- **Entry:** `.squad/decisions/inbox/childs-auth-auto-recovery.md`
- **Key Outcome:** Users need only `az login`, not `az account set` — extension auto-selects first enabled subscription

## Cross-Team Impact
- **For Palmer (DevOps):** Auth now works seamlessly with bundled SDK in packaged vsix
- **For Rob Pitcher (User):** Setup friction reduced — no subscription configuration needed

## Verification
✅ All files staged. Ready for merge.
