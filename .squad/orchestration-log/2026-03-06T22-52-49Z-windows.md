# Orchestration Log — Windows (Tester)

**Timestamp:** 2026-03-06T22:52:49Z  
**Task:** Add tests for auth-method-aware error rewriting  
**Status:** SUCCESS

## Files Modified

- `src/test/error-scenarios.test.ts` (3 new tests)
  - Test: `_rewriteAuthError - entraId branch` — Verifies RBAC guidance message for Entra ID auth errors
  - Test: `_rewriteAuthError - apiKey branch` — Verifies API key message for API Key auth errors
  - Test: `_rewriteAuthError - non-auth error passthrough` — Confirms non-auth errors are unchanged for both auth methods
  
- `src/test/extension.test.ts` (4 new tests + @azure/identity mock fix)
  - Added @azure/identity mock (CLASS definition) to module-level mocks
  - Test: `handles Entra ID auth errors with RBAC guidance` — Full integration with Entra ID credential flow
  - Test: `handles API Key auth errors with key guidance` — Full integration with API Key credential flow
  - Test: `non-auth errors pass through unchanged` — Confirms error filtering for both auth methods
  - Test: `error message format includes descriptive text` — Validates message clarity and actionability

## Outcome

- 7 new tests all passing
- @azure/identity mock fix also resolved 21 pre-existing test failures in the suite
- Full test coverage for both auth method branches in `_rewriteAuthError()`

## Technical Notes

- Mock strategy: @azure/identity must be a CLASS (not factory function) to match SDK import patterns
- Integration tests use full credential provider lifecycle (createCredentialProvider call + entraId/apiKey branches)
- All tests properly cleaned up with mock resets to prevent test pollution
