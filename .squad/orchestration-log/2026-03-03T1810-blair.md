# Blair (Extension Dev) — 2026-03-03T18:10Z

**Triggered by:** Rob Pitcher — address PR #129 review comments

## Work Completed

### 1. Fixed Multi-subscription Guardrail in autoSelectSubscription()
- **File:** `src/auth/credentialProvider.ts`
- **Issue:** Blindly selected first enabled subscription on multi-subscription machines, mutating Azure CLI global state
- **Fix:** Changed logic to only auto-select when exactly one enabled subscription exists; returns `boolean` (was `void`)
- **Error handling:** Returns false with actionable message for zero or multiple subscriptions

### 2. Fixed Command Injection in autoSelectSubscription()
- **File:** `src/auth/credentialProvider.ts`
- **Issue:** Used `execSync` with shell string interpolation for `az` CLI calls
- **Fix:** Replaced all `execSync` calls with `execFileSync(binary, [...args], { timeout: 10000 })`
- **Timeout:** Added 10-second timeout to prevent hung `az` CLI from blocking extension host indefinitely

### 3. Fixed execFileSync Mock Unsafe Casts
- **File:** `src/test/auth/credentialProvider.test.ts`
- **Issue:** Test mocks used unsafe casts like `(mockExecFileSync as any)`
- **Fix:** Used `vi.mocked()` wrapper with proper type inference
- **Impact:** Improved test type safety and compliance with mock patterns

### 4. CLI Preflight Validation Integration
- **File:** `src/extension.ts`
- **Added:** `performCliPreflight()` function during activation
- **Calls:** Childs' `discoverAndValidateCli()` from copilotService
- **UX:** Shows VS Code warning with context-specific buttons ("Open Settings", "Open Terminal")
- **Webview:** Posts `cliStatus` message to show in-chat banner

### 5. Fixed "double v" Version String Bug
- **Location:** CLI validation error message
- **Issue:** Typo in version check failure message
- **Fix:** Corrected to single "v" in error text

## Decisions Made

1. **execFileSync pattern for Azure CLI:** Established best practice for all `child_process` calls in auth layer
2. **Preflight non-blocking:** CLI validation runs async during activation, doesn't slow extension startup
3. **Fix-it UX pattern:** Reused existing auth banner CSS and message pattern for consistency

## Status

✅ **Complete** — Code committed directly  
✅ Typecheck: `npx tsc --noEmit` passes  
✅ Tests: All 246 tests pass  
⚠️ Note: Pre-existing highlight.js bundling issue on branch (unrelated to PR #129 fixes)

## Coordinated With

- **Childs:** CLI validation functions (`validateCopilotCli`, `discoverAndValidateCli`)
- **Windows:** Updated test mocks to use `vi.mocked()` pattern
