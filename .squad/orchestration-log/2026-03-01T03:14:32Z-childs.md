# Orchestration Log: Childs (2026-03-01T03:14:32Z)

**Agent:** Childs (SDK Dev)  
**Session:** Background  
**Task:** Fixed authStatusProvider.ts — Entra ID "not logged in" error classification  
**Status:** ✅ Complete

## Summary

Fixed `src/auth/authStatusProvider.ts` to distinguish between "not logged in" (normal state) and "actual errors" (configuration/network issues).

### Changes
- **isNotLoggedInError()** helper — 8 regex patterns for DefaultAzureCredential signals
- **checkAuthStatus()** — Routes credential errors to `notAuthenticated` (friendly) or `error` (short message)
- **Tests:** 4 new test cases, 2 existing updated, all 17 tests pass

### Decision
Entra ID error classification into two buckets: "not logged in" → `notAuthenticated` state; actual errors → `error` state with friendly message (no raw SDK stack).

### Impact
- UI shows correct sign-in prompt for first-run users (Blair's banner now displays correctly)
- Prevents exposing internal SDK details to end users
- Type-compatible with existing `AuthStatus` interface

**Files:** `src/auth/authStatusProvider.ts`, `src/auth/__tests__/authStatusProvider.test.ts`
